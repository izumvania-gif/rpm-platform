import { PersonaStub } from '@/components/shared/persona-stub'

export default function PublicDashboardPage() {
  return (
    <PersonaStub
      title="Компания"
      tagline="Открытый дашборд без входа — какие есть продукты, кто ими управляет, что публично в роадмапе."
      planned={[
        'Список продуктов с ответственным PM и публичным описанием (уже есть в модели — Фаза 4 подключит эту страницу)',
        'Только публичные пункты роадмапа (без внутренних деталей)',
        'Единственный по-настоящему открытый маршрут — строится без предположений о сессии, с явным белым списком полей (см. §4 плана)',
      ]}
    />
  )
}
