// Область отчёта «Пробелы» (фаза 20, plans/2.3-scenario-audit-plan.md).
//
// Обычный модуль, а не экспорт из `'use client'`-файла формы: Next.js
// превращает любой экспорт файла с клиентской границей в непрозрачную ссылку,
// и на сервере `searchParams.productId === ALL_PRODUCTS` молча становился
// ложью — страница показывала активный продукт вместо «всех». Это третий раз,
// когда эта ловушка стреляет в кодовой базе (см. CLAUDE.md, «Mutation
// pattern»).

/** Значение параметра `productId`, означающее «по всем продуктам». */
export const ALL_PRODUCTS = 'all'
