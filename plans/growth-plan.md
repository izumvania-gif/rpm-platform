# Growth Plan: RPM Platform

**Дата обновления:** 6 августа 2026
**Статус:** Часть 1 (текущее состояние) — зафиксировано по факту. Часть 2 (план развития) — согласовано с продакт-менеджером, к реализации.

> **Ребрендинг:** продукт переименован из **ECHO** в **RPM Platform** (имя репозитория `rpm-platform` уже ему соответствовало). Документация, UI и код обновлены под новое имя — см. §2.0 «Ребрендинг». Единственное намеренное исключение — `ECHO_functional_requirements.md`, архивный спек-документ под историческим именем.

---

## Северная звезда

Платформа существует, чтобы: собрать всю доступную информацию о продукте → понять, какие реальные задачи (jobs) решают клиенты → связать эти задачи с сегментами, у которых они есть → подтвердить это исследованиями и разговорами → показать, где картина неполная или противоречивая, чтобы продакт-менеджер мог действовать.

Каждая фича ниже должна либо добавлять новую **связанную** информацию, либо помогать **увидеть инсайт/пробел** в уже собранной информации — не быть просто ещё одним списком CRUD.

---

# Часть 1. Текущее состояние

Что реально существует и работает в продукте по состоянию на 6 августа 2026 — снимок на данный момент, а не план. Пункты плана, которые уже сделаны, перенесены сюда из истории заходов; технические детали (схема, решения) сохранены, т.к. они не теряют ценности после реализации.

## 1.1 Модули данных

Десять моделей в `prisma/schema.prisma`, все через `userId` (однопользовательский MVP — `where: { userId }` вручную повторяется в каждом запросе, нет workspace/org-модели):

`Product`, `Research`, `Segment`, `JTBD` (+ `JtbdSequenceEdge` для графа), `Hypothesis` (+ `HypothesisStatusChange`), `Conversation`, `Competitor`, `ProductResource`, `Feature`, `RTB`.

### Методологическое ядро: JTBD → Feature → RTB

```prisma
model Competitor {
  id          String   @id @default(cuid())
  name        String
  url         String?
  positioning String?  @db.Text
  features    String[]
  productId   String
  product     Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Feature {
  id          String   @id @default(cuid())
  name        String
  description String?  @db.Text
  productId   String
  product     Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  jtbds       JTBD[]   // many-to-many: одна фича может закрывать несколько задач
  rtbs        RTB[]    // many-to-many: фича может поддерживать несколько промо-обещаний
}

model RTB {
  id          String    @id @default(cuid())
  statement   String    @db.Text
  productId   String
  product     Product   @relation(fields: [productId], references: [id], onDelete: Cascade)
  userId      String
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  features    Feature[]
}
```

`ProductResource` — одна гибкая модель (`kind`: `SALES_KIT` / `DEVELOPER_DOC` / `CONFLUENCE_LINK` / `JIRA_LINK` / `OTHER`) вместо отдельной таблицы на тип контента; сейчас только заголовок + URL + заметка, без реальной интеграции с Confluence/Jira. Отображается как секция на странице продукта, без отдельного списка.

`Competitor`, `Feature`, `RTB` — полноценные модули (собственный список с сортировкой/CSV-экспортом, карточка, форма, копирование ссылки, "недавно просмотренное", закреплённые записи, дублирование), плюс секция на странице продукта. При первой реализации (Фаза A) были сделаны **без** `pinned`/`tags`/видимости на дашборде — по аналогии с `Product`; при последующем разборе эта аналогия признана слабой (в отличие от `Product`, эти три модуля — кросс-продуктовые списки leaf-записей, как Research/Segment/JTBD, а не корневая точка входа), и пробел закрыт — см. §2.2 "сделано".

### JTBD: иерархия, последовательность, граф, типы задач

```prisma
model JTBD {
  // ...
  parentId String?
  parent   JTBD?       @relation("JtbdHierarchy", fields: [parentId], references: [id], onDelete: SetNull)
  children JTBD[]      @relation("JtbdHierarchy")
  jobType  JtbdJobType @default(SMALL_JOB)
}

enum JtbdJobType {
  BIG_JOB    // продукт не решает полностью, часто про эмоции клиента
  CORE_JOB   // самая крупная задача, которую продукт решает целиком
  SMALL_JOB  // шаг до/после основной
  MICRO_JOB  // узкая задача внутри малой/основной
}

model JtbdSequenceEdge {
  id         String @id @default(cuid())
  fromJtbdId String
  fromJtbd   JTBD   @relation("JtbdSequenceFrom", fields: [fromJtbdId], references: [id], onDelete: Cascade)
  toJtbdId   String
  toJtbd     JTBD   @relation("JtbdSequenceTo", fields: [toJtbdId], references: [id], onDelete: Cascade)
  @@unique([fromJtbdId, toJtbdId])
}
```

- Иерархия: один родитель, произвольная глубина, дерево через `parentId`. `setJtbdParent` (`lib/actions/jtbd-graph.ts`) защищён от циклов и самоссылок (`isDescendant`).
- Последовательность: отдельный граф рёбер "следует за", независимый от уровня иерархии. `createJtbdSequenceEdge` защищён только от самоссылки — не от встречной пары (A→B и B→A одновременно) и не от циклов длиннее двух узлов. Осознанно не исправляется сейчас (см. §2.5).
- Канвас `/jtbd/graph` — `@xyflow/react` (React Flow), единственная новая зависимость сверх стандартного UI-стека. Узлы окрашены по `jobType` (легенда на канвасе), перетаскивание меняет `parentId`, соединение рёбер создаёт `JtbdSequenceEdge`, есть инлайн-добавление JTBD прямо на канвасе (`createJtbdQuick`, без редиректа), фильтр по категории. `jtbdJobTypeColors` (`lib/jtbd-job-types.ts`) — CSS-переменные темы (`--jtbd-*` в `app/globals.css`, светлая и тёмная версии), не хардкод hex (исправлено, см. §2.2).
- Обычный список `/jtbd` (группировка по категориям, % покрытия) остаётся отдельно от канваса.
- Нет валидации согласованности `jobType` с позицией в иерархии (например, ничто не мешает вложить `BIG_JOB` под `MICRO_JOB`) — осознанно не ограничивается сейчас (см. §2.5).

### Гипотезы: канбан

`/hypotheses` — карточки перетаскиваются между колонками статуса нативным HTML5 Drag and Drop (`components/hypotheses/kanban-board.tsx`, без новой зависимости), вызывает существующий `updateHypothesisStatus`, пишет в `HypothesisStatusChange`. Кнопки статуса на карточке гипотезы остаются как запасной путь (мобильные устройства, где DnD неудобен).

### Фичи: фильтры

`/features` — фильтр по JTBD и сегменту (`components/forms/feature-filter-form.tsx`) поверх сортировки. Прямой связи Feature↔Segment нет, фильтр по сегменту идёт через `jtbds.some({ segmentId })`.

## 1.2 Визуальный стиль

Фирменный красно-белый стиль ("Rutoken corporate") — CSS-переменные темы (`--primary`, `--ring`, `--accent`, `--destructive`) в `app/globals.css`, светлая и тёмная версии; `--destructive` заметно темнее `--primary`, чтобы кнопки "Удалить" не сливались с акцентными. `SiteNav` — красная полоса сверху, точка-логотип, подсветка активного раздела.

Карточная перегруппировка ("wall of text" → секции): страница продукта (9 плоских секций → 3 смысловых блока с заголовком-разделителем), дашборд (`Card` вместо голых блоков), карточки JTBD/Гипотезы/Feature/RTB (связанные записи в `Card`), списки `/competitors`/`/features`/`/marketing` (группировка по продукту вместо плоской сетки — см. §2.3, было сделано отдельным заходом).

## 1.3 Текущая навигация

5 главных табов в `SiteNav` (`components/shared/site-nav.tsx`): Продукты (под-меню: Фичи), Сегменты, Исследования (под-меню: Гипотезы, Разговоры), Маркетинг, JTBD (сам содержит переключатель "Список"/"Граф"). Конкуренты — без пункта меню, доступны только со страницы продукта; маршрут `/competitors` не удалён, но не рекламируется. Подробности решений — §2.1.

## 1.4 Мелкие удобства (реализовано целиком)

Недавно просмотренное (localStorage), "мой текущий продукт по умолчанию", закреплённые записи (`pinned` — теперь на всех 8 моделях: Research/Segment/JTBD/Hypothesis/Conversation и, после §2.2, также Competitor/Feature/RTB), дублирование записи (все 8 моделей, после §2.2), CSV-экспорт списков, "скопировать ссылку", версия для печати, сортировка и фильтры в списках, единые теги (тот же охват — все модули с полем `tags` показывают их и в списке, и на карточке), лента активности и история статусов гипотезы на дашборде (после §2.2 — по всем 8 моделям, не только по исходным 6), индикатор "давно не подтверждали", клавиатурные сокращения (`n`, `g`+буква).

`ProductResource` — единственная модель осознанно без `pinned`/`tags`/видимости на дашборде: это не первоклассная запись, а прикреплённый файл/ссылка к продукту (см. §2.2 для полного обоснования).

## 1.5 Демо-данные

`prisma/seed.ts` — продукт «Рутокен CLM» на основе реального сейл-кита: сегменты, исследования, JTBD (с иерархией и `jobType`), гипотезы со статусной историей, разговоры, 2 конкурента, 2 ресурса, 4 фичи, 3 RTB, 3 ребра последовательности JTBD. Идемпотентно.

## 1.6 Осознанно принятые ограничения (не баги, не забытое — решения)

- **Целостность данных:** нет валидации согласованности `jobType` ↔ позиция в иерархии JTBD, нет защиты `JtbdSequenceEdge` от циклов/встречных пар, нет проверки, что `jtbdId`/`segmentId`/`researchId` гипотезы принадлежат одному продукту. Решение продакт-менеджера (6 августа 2026): не усложнять сейчас — платформа не должна быть слишком строгой на этом этапе.
- **Тесты и CI:** нет автотестов, нет `.github/workflows`. Решение продакт-менеджера (6 августа 2026): рано для этого проекта, откладывается.
- **Однопользовательский режим:** `DEFAULT_USER_ID` захардкожен, `where: { userId }` не обеспечивается общим слоем — держать в поле зрения по мере роста, не блокирует текущую работу.

---

# Часть 2. План развития

## 2.0 Ребрендинг: ECHO → RPM Platform — ✅ сделано (код)

Имя продукта обновлено везде, где оно встречалось в коде и документации:

- `app/layout.tsx` — `<title>` метаданных → `"RPM Platform - Research & Product Management"`.
- `components/shared/site-nav.tsx` — логотип в шапке → `RPM.`
- `app/page.tsx` — заголовок дашборда → `RPM Platform`.
- Проверено `grep`-ом по всем `*.ts`/`*.tsx`/`*.js`/`*.jsx` — упоминаний "ECHO" в коде больше нет.

**Не переименовано намеренно:** `ECHO_functional_requirements.md` — исходный спек-документ всё ещё называется по старому имени и расшифровывает акроним ("ECHO — Evidence, Customers, Hypotheses, Outcomes"); остаётся архивным документом под историческим именем, решение о переименовании/ревизии — при следующем содержательном touch этого файла, не блокирует ребрендинг остального продукта.

## 2.1 Информационная архитектура и навигация — ✅ сделано

Вместо плоского списка из 9 пунктов (§1.3) — 5 главных табов, остальные модули стали под-табами:

```
Продукты   Сегменты   Исследования   Маркетинг   JTBD
  │                       │           (было RTB)   │
  ├─ Конкуренты*          ├─ Гипотезы               ├─ Список
  └─ Фичи                 └─ Разговоры               └─ Граф

* Конкуренты не в выпадающем меню — доступны только со страницы продукта.
```

- **`SiteNav`** (`components/shared/site-nav.tsx`) переписан на двухуровневую структуру: 5 главных ссылок, у «Продуктов» и «Исследований» есть выпадающее под-меню (CSS `group-hover`/`group-focus-within`, без JS-состояния — открывается по наведению или фокусу клавиатурой). Активная вкладка подсвечивается и когда открыта страница из под-меню (например, `/features` подсвечивает «Продукты», `/hypotheses` и `/conversations` — «Исследования»), а также когда открыт `/competitors` (подсвечивает «Продукты», хотя пункта в меню нет — см. ниже).
- **Конкуренты не получили пункта меню.** Как и планировалось: раздел "Конкуренты" убран из выпадающего меню "Продукты" — единственная точка входа теперь секция "Конкуренты" на странице продукта (`ProductSection`, уже показывала список + добавление). Маршрут `/competitors` и `/competitors/[id]` **не удалены** — работают как раньше (нужны для ссылок со страницы продукта, CSV-экспорта, дублирования), просто больше не рекламируются в навигации. На самой странице `/competitors` добавлена поясняющая подпись под заголовком: "Обычно открывается из карточки продукта — этот список объединяет конкурентов по всем продуктам сразу".
- **Фичи** — пункт выпадающего меню "Продукты" (`/features`), список и фильтры не менялись.
- **Гипотезы и Разговоры** — пункты выпадающего меню "Исследования" (`/hypotheses`, `/conversations`).
- **JTBD** — отдельный главный таб, без изменений внутри самого модуля кроме под-навигации ниже.
- **Список / Граф внутри JTBD** — новый общий компонент `components/shared/jtbd-view-tabs.tsx` (`JtbdViewTabs`), два таба-ссылки на одном уровне (`/jtbd` и `/jtbd/graph`), активный подсвечен заливкой `bg-primary`. Заменил собой асимметричную пару кнопок "Граф JTBD" (на `/jtbd`) и "Обычный список" (на `/jtbd/graph`).
- **RTB → «Маркетинг»: маршрут переименован.** `app/rtb/**` перенесён в `app/marketing/**` (`/marketing`, `/marketing/[id]`, `/marketing/[id]/edit`, `/marketing/new`), все внутренние ссылки и `revalidatePath`/`redirect` в `lib/actions/rtbs.ts` обновлены. Модель Prisma осталась `RTB` (не переименована — решение принято: технические имена не обязаны совпадать с UI-лейблами, миграция схемы того не стоит). Заголовок списка — «Маркетинг» с подписью "RTB (Reasons To Believe) — маркетинговые обещания, опирающиеся на фичи продукта"; кнопка создания и заголовки создания/редактирования остались "Новый RTB"/"Редактировать RTB" — типизация конкретной записи, а не название раздела. Секция на странице продукта тоже переименована в «Маркетинг» (`app/products/[id]/page.tsx`). Старый `/rtb` **не оставлен как редирект** — маршрут удалён физически, обращение к нему теперь 404 (сознательно: это внутренний MVP-инструмент без внешних пользователей с закладками, оставлять мёртвый алиас не было смысла).
- **Клавиатурные сокращения** (`components/shared/keyboard-shortcuts.tsx`) обновлены вместе с навигацией: `g k` (конкуренты) убран — как и сама вкладка, скрытый шорткат на ненавигируемый раздел был бы confusing; добавлен `g m` → `/marketing` (у RTB раньше не было буквы принципиально, но "Маркетинг" даёт понятную мнемонику). `n` на `/marketing` создаёт новую запись как раньше.

## 2.2 UX-паритет между модулями — ✅ сделано

Все пробелы, найденные при разборе текущего состояния, закрыты:

- **`RecentlyViewedTracker` на карточке Разговора** (`app/conversations/[id]/page.tsx`) — добавлен, теперь на всех 9 детальных страницах.
- **Теги в списках, не только на карточках** — `TagBadges` добавлен в списки `/jtbd` (под заголовком записи), `/segments` (в карточке), `/research` (отдельная колонка "Теги"), как уже было на `/conversations`.
- **"Создать похожую" (дублирование) на Competitor/Feature/RTB** — добавлена кнопка "Дублировать" на детальных страницах, страницы `/new` каждого модуля читают `?duplicateFrom=` и предзаполняют форму (включая связи many-to-many у Feature/RTB), по той же схеме, что уже была на JTBD/Research/Segment/Hypothesis/Conversation. Проверено end-to-end (Playwright): переход по "Дублировать" корректно предзаполняет форму данными исходной записи.
- **`pinned` + видимость на дашборде для Competitor/Feature/RTB** — добавлено поле `Boolean pinned` в схему (`prisma/schema.prisma`), `togglePinned`-экшены (`lib/actions/{competitors,features,rtbs}.ts`), кнопка-звёздочка на списках и карточках, и все три модели подключены к ленте активности и блоку "закреплённое" на дашборде (`app/page.tsx`) — дашборд теперь агрегирует по всем 9 моделям, а не по исходным 6. Проверено end-to-end: клик по звёздочке персистится через Server Action и сразу отражается на дашборде. `ProductResource` осознанно остаётся без этого — не первоклассная запись, а прикреплённый файл/ссылка к продукту.
- **Тёмная тема для цветов типов задач JTBD** — `jtbdJobTypeColors` (`lib/jtbd-job-types.ts`) переведён на CSS-переменные темы (`--jtbd-big-bg`/`-text`/`-border` и аналоги для core/small/micro, заданы в `app/globals.css` для `:root` и `.dark`) вместо хардкод hex. `JobTypeBadge` и узлы графа (`JtbdNode`) не менялись — они уже читали цвета из `jtbdJobTypeColors`, поэтому подхватили тему автоматически. Проверено визуально (принудительно применённая тёмная палитра): цвета всех 4 типов задач остаются контрастными и читаемыми в тёмном режиме.

## 2.3 Карточная перегруппировка: Конкуренты, Фичи, Маркетинг — ✅ сделано

Списки `/competitors`, `/features`, `/marketing` были плоской сеткой карточек по всем продуктам сразу — самый "плоский" паттерн из оставшихся на платформе (в отличие от `/jtbd`, который уже группировал по категории). Приведены к тому же принципу, что и `/jtbd`: **группировка по продукту**, а не по абстрактным зонам-разделам (как на странице продукта, §1.2) — этим трём модулям, в отличие от Product, зонирование не подходит, это списки leaf-записей, а не аггрегатор с несколькими смысловыми блоками.

- Каждая страница строит `Map` продукт → записи (тот же паттерн, что `byCategory` на `/jtbd`), сортирует группы по названию продукта (`localeCompare('ru')`), рендерит заголовок `<h2>` с названием продукта и счётчиком перед каждой сеткой карточек — визуально идентично блокам категорий на `/jtbd`.
- Убрано дублирование: раз карточки теперь сгруппированы под заголовком продукта, `CardDescription` с названием продукта внутри каждой карточки стала избыточной и удалена (для RTB/"Маркетинг" на её месте остался счётчик фич).
- Добавлена сводная строка над списком по образцу `/jtbd` ("N продуктов · M записей"), которой раньше не было ни на одной из трёх страниц.
- Сортировка (там, где есть — `/competitors`, `/features`) и фильтры (`/features`: JTBD/сегмент) применяются к полному набору записей до группировки — группы получаются стабильно упорядоченными.
- Проверено визуально на демо-данных (пока один продукт «Рутокен CLM» — с ростом числа продуктов эффект группировки станет заметнее).

## 2.4 Инсайты и связность (доп. предложения, приоритет ниже — без изменений с прошлого захода)

- **Матрица "Сегменты × JTBD"** — кросс-таблица: какой сегмент какие задачи имеет и насколько это подтверждено исследованиями.
- **Дашборд пробелов (gap analysis)** — JTBD без подтверждающего исследования, сегменты без единого JTBD, гипотезы, зависшие в "черновике", продукты без исследований за N дней.
- **Цитаты/выводы как отдельные записи** — атомарные инсайты (короткий текст + источник + привязка к сегменту/JTBD) вместо одного большого транскрипта на `Conversation`.
- **Глобальный поиск** — при 10 связанных модулях находить конкретное кликами по спискам перестаёт работать.
- **Авторизация/мультипользовательность** — не срочно, держать в поле зрения по мере роста.

## 2.5 Явно отклонённые предложения (зафиксировано, чтобы не поднимать заново)

По итогам разбора текущего состояния 6 августа 2026 продакт-менеджер явно решил **не делать сейчас**:

- Валидацию согласованности `jobType` и позиции в иерархии JTBD.
- Защиту `JtbdSequenceEdge` от циклов/встречных пар.
- Кросс-валидацию связанных записей гипотезы (`jtbdId`/`segmentId`/`researchId` из одного продукта).
- Любую другую дополнительную "жёсткость" модели данных — платформа должна оставаться гибкой на этом этапе.
- Автотесты и CI/CD — рано для текущей стадии проекта.

Причина зафиксировать явно: чтобы будущий разбор кода не поднимал эти пункты как "забытые баги" — это осознанные решения с датой.

## 2.6 Онбординг и упрощённый ввод данных (не сделано, без изменений с прошлого захода)

Из предыдущего плана — ещё не реализованные пункты:

- **D.2 Быстрый захват** — глобальная кнопка "+" в шапке, модалка выбора типа записи, инлайн-форма с обязательными полями.
- **D.3 Мастер настройки нового продукта** — короткая необязательная последовательность шагов после создания продукта (сегмент → JTBD → конкурент), с возможностью пропустить.
- **D.4 Массовый ввод (paste-list/CSV)** — на страницах списка (Сегменты, Конкуренты, JTBD) — текстовое поле "одна запись на строку", создание N записей одним проходом.
- **D.6 Дублирование структуры продукта** — "Создать продукт на основе..." копирует категории/сегменты (не сами данные) из существующего продукта. Низкий приоритет.

---

## Порядок реализации

1. **Ребрендинг (§2.0) — ✅ сделано.**
2. **UX-паритет (§2.2) — ✅ сделано.**
3. **Навигация (§2.1) + карточная перегруппировка (§2.3) — ✅ сделано.**
4. **👉 Следующее: Фаза "Инсайты и связность" (§2.4)** — по одному пункту за раз, по мере необходимости.
5. **Онбординг (§2.6)** — по мере запроса, не блокирует остальное.

Данные-интеграционные ограничения (§2.5) и тесты/CI — сознательно вне очереди, не планируются на обозримый срок.

---

## Открытые вопросы на будущее (не блокируют старт)

- Feature ↔ RTB: точная семантика many-to-many устраивает, или RTB должен быть привязан к одной конкретной фиче?
- Нужен ли на канвасе JTBD отдельный "read-only" режим, или редактирование всегда доступно всем, у кого есть доступ к платформе?
- Confluence/Jira: когда реальная интеграция станет актуальна — OAuth от лица пользователя или сервисный аккаунт на весь workspace?
- Мастер нового продукта (D.3): показывать всегда при создании продукта, или только при самом первом продукте в системе?
- Массовый ввод (D.4): достаточно ли простого формата "одна запись на строку", или сразу нужна поддержка CSV с несколькими колонками?
