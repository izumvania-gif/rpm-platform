# Growth Plan: расширение платформы ECHO

**Дата:** 6 августа 2026
**Статус:** Согласован с продакт-менеджером (см. раздел "Принятые решения"), к реализации

---

## Северная звезда

Платформа существует, чтобы: собрать всю доступную информацию о продукте → понять, какие реальные задачи (jobs) решают клиенты → связать эти задачи с сегментами, у которых они есть → подтвердить это исследованиями и разговорами → показать, где картина неполная или противоречивая, чтобы продакт-менеджер мог действовать.

Каждая фича ниже должна либо добавлять новую **связанную** информацию, либо помогать **увидеть инсайт/пробел** в уже собранной информации — не быть просто ещё одним списком CRUD.

---

## Принятые решения

| Вопрос                                                                            | Решение                                                                                                                                        |
| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Модель ресурсов продукта (sales-kit, dev-docs, будущие ссылки на Confluence/Jira) | Одна гибкая модель `ProductResource` с полем `kind` (enum), а не отдельная модель на каждый тип                                                |
| Модель конкурентов                                                                | Отдельная первоклассная модель `Competitor` (не через `ProductResource`)                                                                       |
| Глубина иерархии JTBD                                                             | Произвольная глубина (большая задача → задача → микро-задача → ...), один родитель на запись, у родителя может быть много детей                |
| Последовательность JTBD                                                           | Отдельная связь "следует за" (граф рёбер), независимая от иерархии; применима на любом уровне вложенности, не только к задачам верхнего уровня |
| UI графа JTBD                                                                     | Полноценный визуальный канвас (drag-and-drop, соединение узлов) — сразу, не откладывается на вторую фазу                                       |
| Слой Feature → RTB                                                                | Включить в эту фазу — это методологическое ядро платформы (JTBD → Feature → RTB), сейчас отсутствует полностью                                 |

---

## Фаза A: Контекст продукта и методологическое ядро — ✅ сделано

> Все три модуля реализованы по схеме из этого раздела без отклонений. `Competitor`, `Feature`, `RTB` — полноценные модули (`/competitors`, `/features`, `/rtb`: список с сортировкой/CSV-экспортом, карточка, форма, копирование ссылки, "недавно просмотренное" — без `pinned`/`tags`, как и `Product`, по той же логике). `ProductResource` — без отдельного списка, только секция на странице продукта с инлайн-редактированием/удалением, как и было решено. Feature↔JTBD и Feature↔RTB — реализованы как implicit many-to-many (чекбоксы в форме); на карточке JTBD появился блок "Фичи, которые закрывают". Демо-продукт «Рутокен CLM» обогащён 2 конкурентами, 2 ресурсами, 4 фичами и 3 RTB на основе сейл-кита. Добавлены пункты нав-меню и клавиатурные сокращения (`g k` → конкуренты, `g f` → фичи; RTB — без буквы, чтобы не гадать с мнемоникой).

### A.1 `Competitor` — конкуренты

Первоклассная модель, привязанная к продукту.

```prisma
model Competitor {
  id          String   @id @default(cuid())
  name        String
  url         String?
  positioning String?  @db.Text   // позиционирование, профиль
  features    String[]            // краткий список фич конкурента (теги)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  productId   String
  product     Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

UI: список/карточки на отдельной странице `/competitors` (как Segments) + секция на странице продукта, как у остальных модулей.

### A.2 `ProductResource` — sales-kit, dev-docs, будущие интеграции

Одна гибкая модель вместо отдельной таблицы на каждый тип контента.

```prisma
enum ProductResourceKind {
  SALES_KIT
  DEVELOPER_DOC
  CONFLUENCE_LINK
  JIRA_LINK
  OTHER
}

model ProductResource {
  id          String              @id @default(cuid())
  title       String
  kind        ProductResourceKind
  url         String?
  description String?             @db.Text
  createdAt   DateTime            @default(now())
  updatedAt   DateTime            @updatedAt

  productId   String
  product     Product             @relation(fields: [productId], references: [id], onDelete: Cascade)
  userId      String
  user        User                @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

**Confluence/Jira — важно:** на этом этапе `CONFLUENCE_LINK`/`JIRA_LINK` — это просто заголовок + URL + заметка, без реального API-подключения (OAuth, синхронизация страниц/задач). Когда понадобится живая интеграция, эти два `kind` "выделяются" в собственные модели с реальными полями (project key, space id, статус синхронизации) — по той же логике, по которой авторизация была отложена, но модель `User` осталась нетронутой для лёгкого возврата.

UI: секция "Ресурсы" на странице продукта, с фильтром по `kind`; форма создания — простая (title, kind-select, url, description).

### A.3 `Feature` и `RTB` — ядро методологии JTBD → Feature → RTB

Сейчас у JTBD нет способа связаться с "как продукт это решает" — это половина заявленной цели платформы. Добавляем два новых уровня.

```prisma
model Feature {
  id          String   @id @default(cuid())
  name        String
  description String?  @db.Text
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  productId   String
  product     Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  jtbds       JTBD[]   // many-to-many: одна фича может закрывать несколько задач
  rtbs        RTB[]    // many-to-many: фича может поддерживать несколько промо-обещаний
}

model RTB {
  id          String    @id @default(cuid())
  statement   String    @db.Text  // формулировка обещания
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  productId   String
  product     Product   @relation(fields: [productId], references: [id], onDelete: Cascade)
  userId      String
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  features    Feature[] // many-to-many
}
```

`JTBD` получает поле `features Feature[]` (обратная сторона many-to-many).

UI: `/features` и `/rtb` как отдельные модули (список/карточка/форма, как остальные), плюс на странице JTBD — список привязанных фич, на странице Feature — список задач, которые она закрывает, и RTB, которые на ней основаны. Это и есть "connecting all the information": один клик с задачи клиента до маркетингового обещания, которое из неё выросло.

---

## Фаза B: Граф JTBD

### B.1 Иерархия

```prisma
model JTBD {
  // ...существующие поля...
  parentId String?
  parent   JTBD?   @relation("JtbdHierarchy", fields: [parentId], references: [id], onDelete: SetNull)
  children JTBD[]  @relation("JtbdHierarchy")
}
```

Один родитель, произвольная глубина, у родителя может быть много детей — обычное самоссылающееся дерево, никаких ограничений на уровень вложенности в схеме.

### B.2 Последовательность

Отдельная сущность "рёбер" — задача A предшествует задаче B. Не дерево (у шага процесса может быть несколько предшественников/преемников), не привязана к уровню иерархии.

```prisma
model JtbdSequenceEdge {
  id         String   @id @default(cuid())
  fromJtbdId String
  fromJtbd   JTBD     @relation("JtbdSequenceFrom", fields: [fromJtbdId], references: [id], onDelete: Cascade)
  toJtbdId   String
  toJtbd     JTBD     @relation("JtbdSequenceTo", fields: [toJtbdId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())

  @@unique([fromJtbdId, toJtbdId])
}
```

### B.3 Визуальный канвас

Узлы = задачи JTBD (сгруппированы визуально по категории/продукту), рёбра — двух видов:

- **иерархия** (`parent`/`children`) — например, пунктирная линия или вложенность
- **последовательность** (`JtbdSequenceEdge`) — сплошная линия со стрелкой в направлении процесса

**Библиотека:** [`@xyflow/react`](https://reactflow.dev/) (React Flow) — стандартный выбор для этой задачи в React-экосистеме, MIT-лицензия, TypeScript, поддерживает кастомные узлы, drag-and-drop, соединение рёбер "из коробки". Это единственная существенная новая зависимость во всём growth-плане — оправдана тем, что канвас с нуля переизобретать не имеет смысла, в отличие от остального UI платформы, написанного вручную.

**Функционал канваса:**

- Просмотр: все JTBD продукта как узлы, иерархия и последовательность как рёбра разных стилей
- Редактирование: перетащить узел под другого — меняет `parentId`; соединить два узла стрелкой — создаёт `JtbdSequenceEdge`
- Клик по узлу — переход на карточку JTBD (с сегментом/исследованием/гипотезами)
- Фильтр по категории, т.к. у продукта может быть много JTBD

Страница: `/jtbd/graph` (или `/products/[id]/jtbd-graph` в контексте продукта) — отдельная от обычного списка `/jtbd`, который остаётся как есть (группировка по категориям, % покрытия) для быстрого сканирования без визуальной сложности канваса.

---

## Фаза C: Инсайты и связность (доп. предложения)

Из вашего запроса "add other suggestions, keep the main purpose in mind" — ниже фичи, которые не запрашивались явно, но напрямую бьют в "connecting information → actionable insights". Приоритет ниже, чем у фаз A/B; можно перемешать порядок по факту.

- **Матрица "Сегменты × JTBD"** — кросс-таблица: какой сегмент какие задачи имеет и насколько это подтверждено исследованиями. Прямой ответ на "understand clients and their problems, segment them" в одном экране вместо перехода по карточкам.
- **Дашборд пробелов (gap analysis)** — автоматический, не ручной: JTBD без подтверждающего исследования, сегменты без единого JTBD, гипотезы, зависшие в "черновике", продукты без исследований за N дней. Это самый прямой способ дать "actionable insights" без всякого AI — просто запрос по уже обязательным связям.
- **Цитаты/выводы как отдельные записи.** Сейчас `Conversation` — один большой транскрипт, а у `Research` нет атомарных "Выводов". Вынесение цитат/инсайтов (короткий текст + источник + привязка к сегменту/JTBD) — то, что реально "соединяет" данные: одна цитата может подтверждать несколько JTBD, один вывод — относиться к нескольким сегментам.
- **Глобальный поиск** — при 8+ связанных модулях находить "то самое про возражения по цене" кликами по спискам перестаёт работать.
- **Авторизация/мультипользовательность** — не срочно, но конкурентная разведка и sales-kits — ровно тот контент, из-за которого общий seed-пользователь начинает ощущаться неправильным решением. Держать в поле зрения по мере роста.

---

## Фаза D: Онбординг и упрощённый ввод данных

С ростом числа связанных модулей (Продукты, Исследования, Сегменты, JTBD, Гипотезы, Разговоры, а после Фазы A ещё Конкуренты/Ресурсы/Features/RTB) растёт и цена входа: каждая запись требует перехода на отдельную страницу `/new` и заполнения полной формы. Ниже — как снизить эту цену в двух местах: (1) добавление информации в уже существующий продукт, (2) старт работы с новым продуктом "с нуля".

Часть пунктов не зависит от Фазы A/B и может быть сделана раньше или параллельно — отмечено у каждого.

### D.1 Инлайн-создание связанных записей прямо из формы — ✅ частично сделано

> Реализовано для пикера сегмента (`InlineCreateSegment`) в формах JTBD/Гипотезы/Разговора: некрасирующий Server Action `createSegmentQuick` возвращает созданный сегмент, форма добавляет его в локальный список и сразу выбирает. Остальные пикеры (исследование, JTBD и т.д.) и конкуренты — не сделаны, по той же схеме можно добавить позже.

**Проблема:** заполняя форму JTBD и понимая, что нужного сегмента ещё нет, приходится бросать форму, идти создавать сегмент на `/segments/new`, возвращаться и заполнять JTBD заново.

**Решение:** в каждом select-пикере связанной сущности (сегмент/исследование/JTBD/конкурент и т.д.) — пункт "+ Создать новый" внизу списка, открывающий компактную инлайн-форму (только обязательные поля) прямо на месте, без ухода со страницы. После создания — запись сразу выбрана в пикере.

**Техническая особенность:** существующие Server Actions (`createSegment`, `createJtbd` и т.д.) сейчас всегда заканчиваются `redirect()` — это ломает сценарий "создать и остаться на текущей форме". Нужен второй, некрасирующий вариант действия (например, `createSegmentInline`), который возвращает созданную запись как данные, а не делает redirect. Дублирование минимально — оба варианта используют одну и ту же Zod-схему и Prisma-вызов, отличается только последняя строка.

**Зависимости:** не зависит от Фазы A/B, применимо к уже существующим модулям уже сейчас.

### D.2 Быстрый захват (Quick Capture)

Из функциональных требований (раздел 10.3) — кнопка "+" в любом разделе, открывающая быстрый ввод произвольного артефакта. AI-классификация ("определить самому, куда это относится") — это уже P2 и вне рамок этого документа. Здесь — версия без AI, которая всё равно снимает большую часть трения:

- Глобальная кнопка "+" в шапке (`SiteNav`), доступна с любой страницы
- Открывает модалку: "Что добавляем?" → Исследование / Сегмент / JTBD / Гипотеза / Разговор / Конкурент
- После выбора — тот же сокращённый инлайн-подход, что в D.1: только обязательные поля + выбор продукта (по умолчанию — последний открытый продукт), остальные поля можно дозаполнить позже через обычное редактирование
- Использует те же "инлайн"-варианты Server Actions из D.1

**Зависимости:** не зависит от Фазы A/B.

### D.3 Мастер настройки нового продукта

**Проблема:** после создания продукта пользователь остаётся один на один с пустой страницей — 4-6 пустых секций ("Пока нет исследований", "Пока нет сегментов" и т.д.), и по функциональным требованиям (раздел 12.2) ожидается управляемый онбординг, а не пустой список.

**Решение:** после `createProduct` — не сразу на `/products/[id]`, а на короткий мастер из необязательных шагов:

1. Продукт создан ✓ (уже сделано)
2. Добавьте один-два сегмента (инлайн-форма D.1, можно пропустить)
3. Опишите первую задачу клиента (JTBD, можно пропустить)
4. (после Фазы A) Добавьте конкурента, если уже знаете ключевых игроков
5. Готово → обычная страница продукта

Каждый шаг пропускаем одним кликом. Мастер не блокирует — это направляющая последовательность, не обязательная валидация.

**Зависимости:** шаги 2-3 не зависят от Фазы A/B; шаг 4 — после Фазы A.

### D.4 Массовый ввод (paste-list / CSV)

**Проблема:** у PM почти всегда уже есть список сегментов, список конкурентов или список задач в голове/в другом инструменте — вводить их по одному через полную форму медленно.

**Решение:** на страницах списка (Сегменты, Конкуренты, JTBD) — кнопка "Массовое добавление" рядом с обычной "Новый...": текстовое поле, одна запись на строку (для JTBD — формат `Категория: формулировка`), после отправки — создаётся N записей одним проходом, с переходом на страницу списка и отчётом "создано X, ошибок Y" при проблемах (например, дубликат slug).

**Зависимости:** для Сегментов и JTBD — независимо от Фазы A/B, можно делать сейчас; для Конкурентов — после Фазы A.

### D.5 Приветственный чек-лист на дашборде — ✅ сделано

> Реализовано на странице продукта (не на дашборде — там нет единого "текущего продукта"): `WelcomeChecklist` показывается, пока сумма связанных записей (сегменты/исследования/JTBD/гипотезы) меньше 4, со ссылками на создание и ручным скрытием через localStorage.

Из функциональных требований (раздел 12.2) — скрываемый баннер с шагами. Показывается на главном дашборде, пока у продукта меньше некоторого порога записей (например, 0 сегментов и 0 JTBD): "① Добавьте сегмент ② Опишите первую задачу клиента ③ Запишите первый разговор" — с прямыми ссылками на создание. Скрывается вручную (крестик) или автоматически, когда контента становится достаточно.

**Зависимости:** не зависит от Фазы A/B.

### D.6 Дублирование структуры продукта

Для компаний с несколькими похожими продуктами — "Создать продукт на основе..." при создании нового продукта: копирует категории/сегменты (не сами данные исследований, а структуру — например, названия сегментов и категории JTBD) из выбранного существующего продукта. Экономит время, когда сегментация клиентов пересекается между продуктами.

**Зависимости:** нет, но имеет смысл делать после того, как в системе есть хотя бы один хорошо заполненный продукт для копирования — по сути, низкий приоритет относительно D.1-D.5.

### О приглашении новых пользователей в платформу

Отдельно от ввода данных о продукте — "enrollment" может означать и приглашение новых людей в саму платформу (не только продукт). Это прямо упирается в отложенную авторизацию (см. Фазу C, "Авторизация/мультипользовательность") — пока она не реализована, пригласить коллегу буквально некуда. Возврат авторизации логично объединить с лёгким онбордингом нового пользователя (магическая ссылка / приглашение по email), а не с классической формой регистрации — но это по-прежнему P1/P2-масштаба работа, отдельная от D.1-D.6.

---

## Фаза E: Мелкие удобства — ✅ сделано целиком

Небольшие фичи без новых архитектурных решений — каждая либо вообще не трогает бэкенд, либо требует минимального изменения существующей схемы. Не завязаны на Фазы A/B/C/D, можно делать в любой момент, в том числе прямо сейчас как быстрые win'ы.

### Навигация и контекст

- ✅ **Недавно просмотренное** — блок на дашборде с последними открытыми записями. Полностью на клиенте (localStorage), без изменений в схеме и Server Actions.
- ✅ **"Мой текущий продукт" по умолчанию** — формы создания подставляют последний выбранный продукт вместо пустого селекта. Cookie/localStorage, без БД.
- ✅ **Закреплённые записи (pinned)** — звёздочка на важных JTBD/гипотезах/исследованиях → отдельный блок на дашборде. Один `Boolean` флаг (`pinned`) на существующих моделях.
- ✅ **Дублирование записи** — кнопка "Создать похожую" на JTBD/гипотезе/сегменте/исследовании/разговоре: открывает форму создания, предзаполненную значениями исходной записи (`defaultValues` уже поддерживаются всеми формами). Ноль изменений в схеме.

### Экспорт и шеринг

- ✅ **CSV-экспорт списков** — кнопка "Экспорт" на любой странице списка. В отличие от PDF (P3 в функциональных требованиях, требует библиотеку рендеринга), CSV — просто сериализация уже загруженных данных, без новых зависимостей.
- ✅ **"Скопировать ссылку"** — кнопка рядом с "Редактировать"/"Удалить" на детальных страницах.
- ✅ **Версия для печати** — `@media print` CSS (через встроенный Tailwind `print:` вариант), без изменений в бэкенде. Удобно распечатать/сохранить в PDF профиль продукта целиком перед встречей.

### Организация

- ✅ **Сортировка и фильтры в списках** — уже отмечено как известный пробел (Research без фильтра по статусу/типу в разделе 0 плана MVP); одна из самых дешёвых и при этом часто запрашиваемых доработок — query-параметр + существующий `orderBy`/`where`.
- ✅ **Единые теги на всех сущностях** — сейчас `tags` есть только у `Research` и `Conversation`. Добавить то же поле на `Segment`/`JTBD`/`Hypothesis` + общий фильтр по тегу на дашборде — расширение уже существующего паттерна, не новый.

### Ощущение momentum

- ✅ **Лента активности на дашборде** — "Сегмент X создан 2 часа назад", "Гипотеза Y обновлена вчера". Объединение `createdAt`/`updatedAt` всех моделей в одну хронологию, без новых таблиц.
- ✅ **История статусов гипотезы** — лог переходов статуса с датами на карточке гипотезы. Единственный пункт в этой фазе, требующий новую (маленькую) таблицу:

  ```prisma
  model HypothesisStatusChange {
    id           String           @id @default(cuid())
    hypothesisId String
    hypothesis   Hypothesis       @relation(fields: [hypothesisId], references: [id], onDelete: Cascade)
    status       HypothesisStatus
    changedAt    DateTime         @default(now())
  }
  ```

  Пишется в неё уже существующий `updateHypothesisStatus` — плюс один `create` рядом с уже имеющимся `update`.

- ✅ **Индикатор "давно не подтверждали"** — визуальная метка на JTBD/исследовании старше N месяцев. Условие по дате в уже читаемых списках, без новых запросов.

### Скорость работы

- ✅ **Клавиатурные сокращения** — `n` — новая запись на странице списка/детальной странице раздела, `g`+буква — переход по разделам (`g d` дашборд, `g p` продукты, `g r` исследования, `g s` сегменты, `g j` JTBD, `g h` гипотезы, `g c` разговоры). Чисто фронтенд.

---

## Демо-данные: пример продукта — ✅ сделано (вне исходного плана)

Не было в исходном плане, добавлено по ходу работы. `prisma/seed.ts` теперь создаёт полностью заполненный пример продукта **«Рутокен CLM»**, построенный на реальном внутреннем сейл-ките (сегменты, исследования, JTBD, гипотезы со статусной историей, CustDev-разговоры) — платформа демонстрирует все модули и фичи Фазы E (теги, пины, staleness-индикаторы, лента активности) на осмысленных данных сразу после `npm run db:seed`, а не на пустых списках. Идемпотентно — пропускает создание, если продукт уже существует.

**Задел на будущее — ✅ выполнено:** после Фазы A демо-продукт обогащён 2 конкурентами (самописное решение vs. встроенный модуль УЦ — прямо из раздела "Конкурентное окружение" сейл-кита), 2 ресурсами (sales-kit, техническая архитектура), 4 фичами и 3 RTB, связанными с существующими JTBD.

---

## Порядок реализации

1. **Фаза E + Фаза D (частично) — ✅ сделано** — самый дешёвый выигрыш по UX без новых моделей данных: мелкие удобства (Фаза E, целиком) вместе с D.1 (инлайн-создание — сделано для пикера сегмента) и D.5 (приветственный чек-лист). Не сделаны в этом заходе: D.2 (быстрый захват / глобальная кнопка "+"), D.3 (мастер настройки нового продукта), D.4 (массовый ввод) — остаются в очереди.
2. **Фаза A — ✅ сделано** — три новых модуля (`Competitor`, `ProductResource`, `Feature`+`RTB`), по уже отработанному паттерну. Закрывает методологическое ядро платформы (JTBD → Feature → RTB).
3. **👉 Следующее: Фаза B** — граф JTBD: сначала схема (`parentId` + `JtbdSequenceEdge`), затем канвас на React Flow. Более рискованная часть — первая новая зависимость и первый нетривиальный UI-компонент в проекте.
4. **Фаза C** — по одному пункту за раз, по мере необходимости; не требует новых архитектурных решений, использует то, что уже есть.
5. **Фаза D (остальное)** — теперь разблокировано Фазой A: шаг 4 мастера (D.3, конкурент) и массовый ввод конкурентов (D.4) можно делать в любой момент; дублирование продукта (D.6) — низкий приоритет, в конце.

---

## Открытые вопросы на будущее (не блокируют старт)

- Feature ↔ RTB: точная семантика many-to-many устраивает, или RTB должен быть привязан к одной конкретной фиче?
- Нужен ли на канвасе JTBD отдельный "read-only" режим (например, для шаринга с не-PM), или редактирование всегда доступно всем, у кого есть доступ к платформе?
- Confluence/Jira: когда реальная интеграция станет актуальна — OAuth от лица пользователя или сервисный аккаунт на весь workspace?
- Мастер нового продукта (D.3): показывать всегда при создании продукта, или только при самом первом продукте в системе (для последующих продуктов PM уже понимает интерфейс)?
- Массовый ввод (D.4): достаточно ли простого формата "одна запись на строку", или сразу нужна поддержка CSV с несколькими колонками (например, сегмент + доля аудитории + цвет одной строкой)?
