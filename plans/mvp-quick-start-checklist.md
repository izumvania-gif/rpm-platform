# MVP Quick Start Checklist

Краткий чек-лист для быстрого старта разработки MVP платформы ECHO.

> **Обновлено 6 августа 2026** по факту реализации. См. также раздел 0 [Development Plan](./mvp-development-plan.md#0-статус-реализации-и-отклонения-от-плана) для объяснения отклонений.

---

## Фаза 0: Подготовка инфраструктуры ⚙️ — ✅ ЗАВЕРШЕНА

### Настройка окружения

- [x] Установить Node.js 20+ и npm/yarn
- [x] Установить PostgreSQL 15+
- [x] Установить Git
- [ ] Установить VS Code с расширениями (Prisma, ESLint, Prettier) — на усмотрение разработчика

### Инициализация проекта

- [x] Создать Next.js проект
- [x] Инициализировать Git
- [x] Создать `.gitignore` (включает `.env.local`, `node_modules`, `.next`)
- [x] Первый коммит

### Установка зависимостей

- [x] Основные зависимости (`@prisma/client`, `next-auth`, `@tanstack/react-query`, `zod`, `react-hook-form`, `@hookform/resolvers`, `date-fns`, `lucide-react`)
- [x] Dev-зависимости (`prisma`, `@types/node`, `tsx` — добавлен позже для `prisma db seed`)
- [ ] ~~`npx shadcn-ui@latest init`~~ — не использовалось, компоненты `components/ui/*` написаны вручную (CLI обращается к внешнему registry, недоступному в среде разработки)

### Настройка Prisma

- [x] Инициализировать Prisma
- [x] Настроить `DATABASE_URL` в `.env.local`
- [x] Создать схему БД в `prisma/schema.prisma`
- [x] Синхронизировать схему: `npm run db:push` (вместо `prisma migrate dev` — миграции пока не заведены, `/prisma/migrations` в `.gitignore`)
- [x] Проверить в Prisma Studio: `npm run db:studio`

### Настройка NextAuth.js

- [x] Создать `lib/auth.ts`
- [x] Создать `app/api/auth/[...nextauth]/route.ts`
- [x] Добавить `NEXTAUTH_SECRET` в `.env.local`
- [x] Настроить провайдер Credentials
- ⚠️ Настроено, но **не подключено** ни к одной странице — см. Фазу 1 ниже

### Создание структуры проекта

- [x] `components/ui/` (написаны вручную: button, input, label, textarea, select, card, badge)
- [x] `components/forms/` (product-form, research-form, segment-form, jtbd-form, hypothesis-form)
- [ ] `components/layouts/` — осталась пустой директорией-плейсхолдером, вся навигация в одном `components/shared/site-nav.tsx`
- [x] `lib/prisma.ts`
- [x] `types/` (`types/next-auth.d.ts`)

**Milestone 1 достигнут:** Инфраструктура готова ✅

---

## Фаза 1: Аутентификация и базовый UI 🔐 — ⚠️ ОТЛОЖЕНА

**Решение:** вместо экрана логина/регистрации используется один seed-пользователь. См. `prisma/seed.ts` и `lib/current-user.ts`. Ничего из списка ниже не реализовано — оставлено как есть на будущее, когда понадобится больше одного пользователя.

### Аутентификация

- [ ] Создать API endpoint `POST /api/auth/register`
- [ ] Создать страницу регистрации
- [ ] Создать страницу входа
- [ ] Создать `LoginForm` компонент
- [ ] Создать `RegisterForm` компонент
- [ ] Протестировать регистрацию и вход

### Базовый Layout — частично закрыто без аутентификации

- [ ] ~~`DashboardLayout`/`Sidebar`/`Header`~~ — вместо этого один `SiteNav` в `app/layout.tsx`, без профиля пользователя
- [ ] Добавить защиту маршрутов (middleware) — не нужно без сессий
- [x] Создать главную страницу дашборда (`app/page.tsx` — счётчики по всем модулям)

### Навигация

- [x] Добавить пункты меню: Продукты, Исследования, Сегменты, JTBD, Гипотезы
- [ ] Реализовать активное состояние пунктов меню
- [ ] ~~Добавить кнопку выхода~~ — не нужно без сессий

**Milestone 2 (аутентификация) НЕ достигнут** — отложен сознательно, см. выше.

---

## Фаза 2: Модуль "Продукты" 📦 — ✅ ЗАВЕРШЕНА

### База данных

- [x] Создать модель `Product` в Prisma
- [x] Создать enum `Stage`
- [x] Синхронизировать схему (`npm run db:push`)

### API → по факту Server Actions (`lib/actions/products.ts`)

- [x] `createProduct(formData)` — создание
- [x] чтение — напрямую через Prisma в Server Component (не отдельный endpoint)
- [x] `updateProduct(id, formData)` — обновление
- [x] `deleteProduct(id)` — удаление

### UI компоненты

- [x] `ProductForm` (create/edit) — с автоподстановкой slug (транслитерация кириллицы)
- [x] Страница списка продуктов
- [x] Страница профиля продукта (+ связанные исследования/сегменты/JTBD/гипотезы)
- [ ] ~~`ProductCard`~~ — карточка встроена прямо в страницу списка, отдельным компонентом не вынесена
- [ ] ~~`StageSelect`~~ — обычный `<Select>` с опциями стадий прямо в форме

### Валидация

- [x] Zod-схема для Product (`lib/actions/products.ts`)
- [x] Валидация в форме (required-поля + Zod на сервере)
- [x] Обработка ошибок (сообщение над формой + перехват уникальности slug)

### Тестирование

- [x] Создать продукт
- [x] Просмотреть профиль
- [x] Отредактировать продукт
- [x] Удалить продукт
- [x] Проверить валидацию (дубликат slug, пустое название)

**Milestone 3 достигнут:** Модуль "Продукты" работает ✅

---

## Фаза 3: Модуль "Исследования" 🔬 — ✅ ЗАВЕРШЕНА (без фильтров/поиска)

### База данных

- [x] Создать модель `Research` в Prisma
- [x] Создать enum `ResearchStatus` и `ResearchType`
- [x] Добавить связь с `Product`
- [x] Синхронизировать схему

### API → по факту Server Actions (`lib/actions/research.ts`)

- [x] список — напрямую через Prisma, без фильтров
- [x] `createResearch(formData)` — создание
- [x] чтение одного — напрямую через Prisma
- [x] `updateResearch(id, formData)` — обновление
- [x] `deleteResearch(id)` — удаление

### UI компоненты

- [x] Список исследований — простая таблица, без отдельного `ResearchFilters`/`ResearchCard`
- [x] `ResearchForm`
- [x] Детальная страница
- [ ] ~~`ResearchFilters`~~ — не реализовано
- [x] Бейдж статуса — через общий `Badge`, отдельного `StatusBadge` нет

### Функции

- [x] Автогенерация номера исследования
- [ ] Фильтрация по статусу и типу — не реализовано
- [x] Сортировка по дате
- [ ] Поиск по названию — не реализовано
- [x] Привязка к продукту (плюс quick-add со страницы продукта, с предзаполненным `productId`)

### Тестирование

- [x] Создать исследование
- [x] Проверить автогенерацию номера
- [ ] ~~Отфильтровать по статусу~~ — фильтра нет
- [x] Открыть детальную страницу
- [x] Отредактировать исследование
- [x] Удалить исследование

**Milestone 4 достигнут:** Модуль "Исследования" работает ✅

---

## Фаза 4: Модуль "Сегменты" 👥 — ✅ ЗАВЕРШЕНА (без предустановленной палитры)

### База данных

- [x] Создать модель `Segment` в Prisma
- [x] Добавить связь с `Product`
- [x] Синхронизировать схему

### API → по факту Server Actions (`lib/actions/segments.ts`)

- [x] список — напрямую через Prisma
- [x] `createSegment(formData)` — создание
- [x] чтение одного — напрямую через Prisma
- [x] `updateSegment(id, formData)` — обновление
- [x] `deleteSegment(id)` — удаление

### UI компоненты

- [x] Список сегментов (карточный вид)
- [x] `SegmentForm` — с автоподстановкой slug (транслитерация кириллицы)
- [x] Карточка с цветовой меткой (встроена в список, не отдельный компонент)
- [x] Детальная страница
- [ ] ~~`ColorPicker`~~ — нативный `<input type="color">` вместо кастомного пикера

### Функции

- [ ] ~~Предустановленные цвета (8-10 вариантов)~~ — свободный выбор цвета вместо палитры
- [x] Валидация доли аудитории (0-100%)
- [x] Уникальность slug в рамках продукта
- [x] Привязка к продукту (плюс quick-add со страницы продукта)

### Тестирование

- [x] Создать сегмент
- [x] Выбрать цвет
- [x] Проверить отображение цвета
- [x] Отредактировать сегмент
- [x] Удалить сегмент

**Milestone 5 достигнут:** Модуль "Сегменты" работает ✅

---

## Фаза 4.5: JTBD, Гипотезы и Разговоры (P1, реализовано раньше срока) 🎯 — ✅ ЗАВЕРШЕНА

Изначально планировались после MVP (раздел 9.1 плана), но были реализованы вместе с P0 по отдельному запросу.

### База данных

- [x] Создать модель `JTBD` в Prisma (title, category, description, confirmed, связи с Product/Segment/Research)
- [x] Создать модель `Hypothesis` в Prisma (statement, status, priority, связи с Product/JTBD/Segment/Research)
- [x] Создать модель `Conversation` в Prisma (title, transcript, date, tags, связи с Product/Segment/Research)
- [x] Создать enum `HypothesisStatus`
- [x] Синхронизировать схему

### Server Actions

- [x] `lib/actions/jtbd.ts` — create/update/delete
- [x] `lib/actions/hypotheses.ts` — create/update/delete/updateStatus
- [x] `lib/actions/conversations.ts` — create/update/delete

### UI компоненты

- [x] `JtbdForm`, `HypothesisForm`, `ConversationForm` — с клиентской фильтрацией зависимых полей (сегмент/исследование/JTBD) по выбранному продукту
- [x] Список JTBD, сгруппированный по категориям, с % подтверждения исследованиями
- [x] Доска гипотез по статусам (упрощённый Kanban, без drag-and-drop)
- [x] Быстрая смена статуса гипотезы кнопками на детальной странице
- [x] Список разговоров (таблица) с тегами
- [x] Детальные страницы и страницы редактирования для всех трёх

### Интеграция

- [x] Счётчики JTBD/гипотез/разговоров на дашборде и странице продукта
- [x] Quick-add ссылки с предзаполненным `productId` (и `jtbdId` для гипотез) со страниц продукта/JTBD
- [x] Пункты меню в навигации

### Тестирование

- [x] Создать JTBD со связью с сегментом и исследованием
- [x] Создать гипотезу со связью с JTBD, сегментом и исследованием
- [x] Создать разговор со связью с сегментом и исследованием, с тегами
- [x] Проверить группировку по категориям и % покрытия
- [x] Провести гипотезу через все статусы на доске

**Milestone 4.5 достигнут:** JTBD, Гипотезы и Разговоры работают ✅

---

## Фаза 5: Связи между модулями 🔗 — ✅ ЗАВЕРШЕНА (без фильтрации по продукту)

### Функции

- [x] Отображение связанных исследований на странице продукта
- [x] Отображение связанных сегментов на странице продукта (плюс JTBD и гипотез)
- [x] Счетчики (количество исследований, сегментов, JTBD, гипотез) — на странице продукта и на дашборде
- [ ] Фильтрация по продукту во всех модулях — не реализована (списки показывают всё сразу)
- [x] Каскадное удаление (`onDelete: Cascade` от Product; `SetNull` для необязательных связей)

### UI компоненты

- [ ] ~~`ProductSelector`~~ — не выделен отдельным компонентом, выбор продукта встроен в каждую форму с клиентской фильтрацией зависимых полей
- [ ] ~~`RelatedResearches`/`RelatedSegments`~~ — списки встроены прямо в `app/products/[id]/page.tsx`, без отдельных компонентов
- [x] Счётчики на карточках/страницах

### Тестирование

- [x] Создать продукт с исследованиями, сегментами, JTBD и гипотезами
- [x] Проверить отображение связей
- [ ] ~~Проверить фильтрацию~~ — нечего проверять, не реализована
- [x] Проверить каскадное удаление

---

## Фаза 6: Полировка и тестирование ✨ — ⚠️ ЧАСТИЧНО (без toast)

### Обработка ошибок

- [x] Создать `ErrorBoundary` — route-level `app/error.tsx`, не переиспользуемый компонент
- [x] Добавить обработку ошибок API — уже было (сообщение над формой при ошибке валидации/уникальности)
- [ ] Добавить Toast уведомления — не реализовано
- [x] Добавить валидацию всех форм — уже было; в процессе тестирования найдены и исправлены 2 бага (см. ниже)

### UI улучшения

- [x] Добавить Loading states — pending-состояние кнопок отправки (`components/shared/submit-button.tsx`, `useFormStatus`)
- [x] Добавить Skeleton screens — `loading.tsx` на всех 6 списочных страницах (`components/shared/loading-skeleton.tsx`)
- [x] Добавить Empty states — уже было на каждом списке
- [x] Добавить диалоги подтверждения удаления — уже было, нативный `confirm()`
- [x] Проверить адаптивную верстку — 375/640/1280px; найден и исправлен реальный баг переполнения навигации

### Финальное тестирование

- [x] Пройти тестовые сценарии — через Playwright по всем 6 модулям (CRUD + граничные случаи), не буквально по шагам раздела 7.1 плана, но эквивалентное покрытие
- [x] Проверить граничные случаи — пустая обязательная форма, дубликат slug, `audienceShare` вне 0–100, каскадное удаление
- [x] Проверить на разных разрешениях — mobile/tablet/desktop
- [ ] Проверить производительность — не делалось (не критично для однопользовательского MVP)
- [x] Исправить найденные баги — 2 найдены и исправлены, см. ниже

### Баги, найденные и исправленные во время тестирования

- [x] **FK-краш** при создании JTBD/Hypothesis/Conversation с пустым необязательным полем (сегмент/исследование/JTBD) — `.optional().or(z.literal(''))` не срабатывал на `''`. Исправлено через `lib/validation.ts`.
- [x] **Тихая порча данных**: пустые `audienceShare`/`priority` сохранялись как `0` вместо `null` по той же причине у `z.coerce.number()`. Исправлено тем же способом.
- [x] **Респонсив**: навигация с 6 пунктами переполняла контейнер на узких экранах (`SiteNav` без `min-w-0`/`overflow-x-auto`). Исправлено.

**Milestone 6 достигнут:** MVP готов к использованию (toast-уведомления — единственное осознанно отложенное) 🎉

---

## Деплой 🚀

### Подготовка

- [ ] Создать production `.env`
- [ ] Настроить переменные окружения
- [ ] Проверить безопасность (secrets, API keys)

### Vercel

- [ ] Создать аккаунт на Vercel
- [ ] Подключить GitHub репозиторий
- [ ] Настроить переменные окружения
- [ ] Задеплоить приложение

### База данных

- [ ] Создать аккаунт на Supabase (или другой PostgreSQL хостинг)
- [ ] Создать базу данных
- [ ] Запустить миграции на production
- [ ] Проверить подключение

### Проверка

- [ ] Протестировать на production
- [ ] Проверить все функции
- [ ] Проверить производительность

---

## Полезные команды

```bash
# Разработка
npm run dev                    # Запуск dev сервера
npx prisma studio             # Открыть Prisma Studio
npx prisma migrate dev        # Создать миграцию
npx prisma generate           # Сгенерировать Prisma Client

# Деплой
npm run build                 # Сборка для production
npm run start                 # Запуск production сервера
npx prisma migrate deploy     # Применить миграции на production

# Утилиты
npm run lint                  # Проверка кода
npm run format                # Форматирование кода
```

---

## Следующие шаги после MVP

1. ~~**Приоритизировать P1 модули:** JTBD, Разговоры, Гипотезы~~ — все три готовы (Фаза 4.5)
2. ~~**Полировка** (Фаза 6)~~ — loading states, skeleton, pending-кнопки, error/404-страницы, адаптивность готовы; осталось только **toast-уведомления**
3. **Собрать feedback** от первых пользователей
4. **Вернуть аутентификацию** (Фаза 1), когда понадобится больше одного пользователя
5. **Фильтрация/поиск в списках** — не реализованы ни в одном модуле
6. **Выводы исследований и timeline-вид** — из функциональных требований, не начаты
7. **Спланировать AI-инфраструктуру** (P2), включая AI-обработку транскриптов разговоров
8. **Добавить аналитику** использования

---

**Удачи! 🚀**
