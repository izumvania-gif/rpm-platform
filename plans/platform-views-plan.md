# План: 5 представлений платформы (PM / CPO / компания / маркетинг / продажи)

Статус: в разработке на ветке `2.0` (см. `git tag v1.0` для точки, откуда
она стартовала). Фаза 0 сделана — см. §8. Остальные фазы не реализованы.
Составлено по запросу — пять
разных ролей должны видеть RPM Platform по-разному: обычный PM (свои 3–4
продукта + команда + процессы + роадмап), CPO (все продукты как экосистема),
открытый дашборд компании, маркетинг-инструменты, быстрый доступ для продаж.
Это самый крупный запрос за всю историю проекта — фактически превращает
однопользовательский инструмент одного PM в инструмент отдела продукта на
компанию, поэтому перед написанием плана были заданы уточняющие вопросы;
зафиксированные ответы — основа архитектуры ниже.

## 0. Зафиксированные решения (из уточняющих вопросов)

1. **Глубина авторизации в этой фазе — имитация, не настоящий RBAC.**
   Переключатель «посмотреть как PM / CPO / маркетинг / продажи» поверх уже
   существующих единых данных (`DEFAULT_USER_ID`), без переписывания
   NextAuth-флоу под пять ролей. Настоящее разграничение доступа — то, когда
   PM физически не может увидеть чужие продукты — сознательно отложено до
   отдельной, более поздней фазы (см. §7 «Вне рамок»). Исключение —
   открытый дашборд компании (см. пункт 3 ниже): он строится по-настоящему,
   без логина, потому что по смыслу запроса обязан быть таким с самого
   начала.
2. **«Люди», которым PM делегирует задачи, — лёгкие карточки-справочник,
   не логин-аккаунты.** Новая модель `Person` (имя, роль/должность,
   команда, аватар) — того же типа запись, что и `Competitor` сегодня:
   есть, чтобы на неё ссылаться и её показывать, не для входа в систему.
3. **Роадмап — отдельная новая модель**, не поле у `Feature`. Пункт
   роадмапа — это не всегда «фича, которую уже строим» (может быть ставка,
   эксперимент, этап исследования), поэтому он ссылается на `Feature`
   опционально, а не обязан быть с ней тем же самым.
4. **Диаграмма процессов/делегирования — отдельная новая модель
   (`ProcessStep`/`ProcessEdge`), которая переиспользует только канвас**
   (React Flow + авторасстановка + сохранение позиций — уже проверенная в
   графе JTBD инфраструктура), не данные JTBD. JTBD остаётся про «какую
   задачу клиента мы решаем»; процесс — про «кто что делает внутри
   команды» — разные домены, смешивать модели не стоит даже ради экономии
   кода.

## 1. Новые модели данных (черновой набросок Prisma)

```prisma
// Лёгкая карточка человека — не логин-аккаунт (см. §0.2)
model Person {
  id        String   @id @default(cuid())
  name      String
  role      String?      // свободный текст должности, напр. "Маркетинг-лид"
  team      String?      // свободная группировка, напр. "Продукт", "Маркетинг"
  email     String?      // только для отображения, не для входа
  avatarUrl String?
  skills    String[]     // тот же паттерн тегов, что уже используется у 6 моделей
  pinned    Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  userId String
  user   User @relation(fields: [userId], references: [id], onDelete: Cascade)

  ownedProducts Product[]      @relation("ProductOwner")
  roadmapItems  RoadmapItem[]
  processSteps  ProcessStep[]
}
```

```prisma
// Product получает поле «ответственный PM» — отдельно от userId
// (userId — это тенант/создатель записи в текущей однопользовательской
// модели, ownerId — это то, кто из персонажей-Person реально ведёт продукт;
// разные оси, их нельзя схлопывать в одну).
model Product {
  // ...существующие поля без изменений...
  ownerId       String?
  owner         Person?  @relation("ProductOwner", fields: [ownerId], references: [id], onDelete: SetNull)
  publicSummary String?  @db.Text // клиентская фраза "что продукт делает для клиента" для открытого дашборда (§4); пусто — на публичной странице падаем на обрезанный `description`
}
```

```prisma
enum RoadmapStatus {
  PLANNED
  IN_PROGRESS
  SHIPPED
  PAUSED
}

enum RoadmapVisibility {
  INTERNAL
  PUBLIC
}

model RoadmapItem {
  id          String            @id @default(cuid())
  title       String
  description String?           @db.Text
  status      RoadmapStatus     @default(PLANNED)
  quarter     String?           // свободный текст "2026 Q3" — не date-range picker,
                                  // пока реальное использование не покажет, что формат нужен точнее
  visibility  RoadmapVisibility @default(INTERNAL)
  pinned      Boolean           @default(false)
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  productId String
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  ownerId String?
  owner   Person? @relation(fields: [ownerId], references: [id], onDelete: SetNull)

  featureId String?             // опционально — не каждый пункт роадмапа уже фича
  feature   Feature? @relation(fields: [featureId], references: [id], onDelete: SetNull)

  jtbdId String?
  jtbd   JTBD? @relation(fields: [jtbdId], references: [id], onDelete: SetNull)

  userId String
  user   User @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

```prisma
// Процесс — один канонический флоу на продукт (в отличие от JTBD-графа
// с раскладкой на представление — тут не нужна вариативность "по сегменту",
// у внутреннего процесса нет сегментов).
model ProcessStep {
  id        String   @id @default(cuid())
  title     String       // напр. "PM планирует кампанию", "Обсуждение с маркетингом"
  x         Float
  y         Float
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  productId String
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  assignedPersonId String?
  assignedPerson   Person? @relation(fields: [assignedPersonId], references: [id], onDelete: SetNull)

  edgesFrom ProcessEdge[] @relation("ProcessFrom")
  edgesTo   ProcessEdge[] @relation("ProcessTo")
}

model ProcessEdge {
  id         String   @id @default(cuid())
  fromStepId String
  fromStep   ProcessStep @relation("ProcessFrom", fields: [fromStepId], references: [id], onDelete: Cascade)
  toStepId   String
  toStep     ProcessStep @relation("ProcessTo", fields: [toStepId], references: [id], onDelete: Cascade)
  label      String?     // напр. "передаёт в", "согласовывает с"
  createdAt  DateTime    @default(now())

  @@unique([fromStepId, toStepId])
}
```

```prisma
// CPO-экосистема (§5): корреляции между продуктами сначала считаются "на
// лету" из уже существующих связей (общие сегменты/категории JTBD) — без
// новой модели. Эта модель — только для *ручной* пометки CPO "вот тут я
// вижу возможность интеграции", когда автоматика недостаточна. Добавляется
// в Фазе 5, не раньше — не строить впрок то, что можно вывести запросом.
model ProductOpportunity {
  id         String   @id @default(cuid())
  productAId String
  productA   Product  @relation("OpportunityA", fields: [productAId], references: [id], onDelete: Cascade)
  productBId String
  productB   Product  @relation("OpportunityB", fields: [productBId], references: [id], onDelete: Cascade)
  note       String   @db.Text
  createdAt  DateTime @default(now())

  userId String
  user   User @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

```prisma
// Быстрые экшн-планы на предсказуемые "пожары" (§3) — обоснование в
// plans/pm-time-allocation-research.md §1: 52% времени PM уходит на
// незапланированное реагирование (ProductPlan, State of Product Management).
// Не система инцидент-менеджмента — просто заранее написанный "что делать",
// чтобы в моменте не тратить время на придумывание реакции с нуля.
model ActionPlan {
  id        String   @id @default(cuid())
  scenario  String   // "Клиент публично жалуется в соцсетях", "Конкурент резко снизил цену"
  trigger   String?  @db.Text // как понять, что сценарий наступил
  steps     String[] // упорядоченный список коротких шагов — Postgres-массив
                       // сохраняет порядок вставки, тот же паттерн, что и
                       // Competitor.features
  tags      String[] // свободные категории ("PR-кризис", "срыв дедлайна") —
                       // тот же паттерн тегов, что у 6 других моделей
  pinned    Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  productId String
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  ownerId String?             // кто координирует реакцию по этому сценарию
  owner   Person? @relation(fields: [ownerId], references: [id], onDelete: SetNull)

  processStepId String?       // опционально — к какому шагу процесса относится
  processStep   ProcessStep? @relation(fields: [processStepId], references: [id], onDelete: SetNull)

  userId String
  user   User @relation(fields: [userId], references: [id], onDelete: Cascade)
}
```

Итого 6 новых моделей (`Person`, `RoadmapItem`, `ProcessStep`, `ProcessEdge`,
`ActionPlan`, `ProductOpportunity` — последняя опциональная, Фаза 5) + 2
новых поля на `Product` (`ownerId`, `publicSummary`).

## 2. Механика «переключателя ролей» (раз авторизация — имитация)

Раз реальных ролей нет, нужен явный, понятный самому себе механизм, а не
незаметная подмена данных:

- Компонент-переключатель в `SiteNav` (рядом с переключателем темы,
  тот же паттерн — маленькая кнопка/дропдаун, состояние в `localStorage`
  через `lib/client-storage.ts`, как и всё остальное персональное в этом
  приложении).
- Четыре персонажных раздела — **отдельные топ-уровневые роуты**
  (`/pm`, `/cpo`, `/marketing`, `/sales`), не query-параметр поверх
  существующих 10 CRUD-разделов: макеты у персонажей принципиально разные
  (PM видит хаб на один продукт; CPO — кросс-продуктовый обзор), это не
  один и тот же экран с другим фильтром.
- Открытый дашборд компании (§4) — **пятый, отдельный публичный роут**
  (`/public`, без входа в переключатель) — по смыслу именно он должен быть
  ссылкой, которую можно скинуть в общий чат компании без объяснений.
- **Явная оговорка, которая должна быть видна и в UI, не только в этом
  документе:** сегодня `/pm`, `/cpo`, `/marketing`, `/sales` доступны
  любому, кто открыл приложение — переключатель ролей это способ
  посмотреть на данные под нужным углом, а не граница доступа. Настоящее
  разграничение («этот PM физически не увидит чужой продукт») — предмет
  отдельной фазы, см. §7.

## 3. PM-представление (`/pm`)

Скоуп на один выбранный продукт за раз (у PM обычно 3–4 — простой
переключатель продукта наверху страницы, тот же паттерн, что уже есть в
`SiteNav`'е поиска). Компоненты:

- **Хаб продукта** — сокращённая версия уже существующей страницы
  `/products/[id]`, с acknowledgement, что сегменты не обязательны (это уже
  и так true в текущей схеме — `Segment` не требует ничего от `Product`
  кроме связи, здесь ничего нового делать не нужно, только явно не
  требовать их наличия в UI PM-хаба).
- **Роадмап продукта** — список/таймлайн `RoadmapItem` по кварталам
  (`quarter`), с бейджем статуса (тот же визуальный паттерн, что и статусы
  гипотезы — `hypothesisStatusTone`/`Icon` уже есть, `roadmapStatusTone`
  добавляется по аналогии), опциональной ссылкой на `Feature`/`JTBD`,
  ответственным `Person`. Создание/редактирование — обычная форма
  (`lib/actions/roadmap.ts`, тот же CRUD-паттерн, что у всех 11 моделей).
- **Командный дашборд / матрица делегирования** — таблица `Person`
  (отфильтрованных по продукту через их активные `RoadmapItem`/
  `ProcessStep`) × их текущая нагрузка (сколько активных пунктов роадмапа
  и шагов процесса на них назначено прямо сейчас). Это делает дашборд
  реально «кто что делает» вместо голого списка контактов — без роадмапа
  из Фазы 1 эта страница была бы пустой визиткой, поэтому роадмап идёт
  раньше в порядке фаз (§8).
  - **Открытый вопрос:** пользователь описал это как «стратегическую
    матрицу» — не уточнено, какие именно оси. Предложенный по умолчанию
    вариант выше (человек × текущая нагрузка) — самый дешёвый в реализации
    и опирается только на уже спроектированные модели; если нужна другая
    ось (напр. компетенция/грейд × доступность), это меняет `Person`
    (например, добавляет структурированное поле уровня вместо/вместе с
    `skills`), но не меняет остальную архитектуру — можно уточнить перед
    Фазой 2, не раньше.
- **Диаграмма процесса** — канвас на базе `ProcessStep`/`ProcessEdge`,
  переиспользующий инфраструктуру `components/jtbd-graph/canvas.tsx`
  (React Flow, drag, кнопка авторасстановки) в новом
  `components/process-graph/`. В отличие от JTBD-графа — один канонический
  layout на продукт (позиция сохраняется прямо на `ProcessStep.x/y`, без
  аналога `JtbdGraphLayout` по представлениям — у процесса нет сегментов,
  вариативность не нужна). Пример из запроса («PM планирует → обсуждает с
  маркетингом → передаёт в PR») — узел = `ProcessStep` с `assignedPerson`,
  ребро = `ProcessEdge` с необязательной подписью действия.
- **Быстрые экшн-планы на непредвиденные ситуации.** Прямое обоснование —
  `plans/pm-time-allocation-research.md` §1: 52% времени PM уходит на
  незапланированное «тушение пожаров» (ProductPlan, State of Product
  Management), а не на заранее продуманную реакцию. `ActionPlan` (см. §1) —
  заранее написанный «если случилось X → команда делает 1, 2, 3, ответственный
  — Y» для предсказуемо повторяющихся нештатных ситуаций (клиент публично
  жалуется, конкурент резко снижает цену, срывается дедлайн релиза,
  критический баг в проде). Смысл не в том, чтобы описать вообще все
  возможные кризисы — а в том, чтобы **для нескольких самых вероятных**
  команда тратила на реакцию минуты («открыл готовый план, назначил
  ответственного, разослал шаги»), а не заново придумывала процесс в
  моменте, под давлением времени. Отображается двумя способами: (а)
  отдельный список экшн-планов продукта — для быстрого доступа именно в
  момент «пожара», это не тот момент, когда у PM есть время искать что-то
  на канвасе; (б) иконка-счётчик на узле диаграммы процесса, если у шага
  есть привязанные планы («что делать, если этот конкретный шаг пошёл не
  так») — контекстная находка для тех, кто разглядывает сам процесс, а не
  срочный доступ. Список (а) — основной путь, (б) — вторичный.

## 4. Открытый дашборд компании (`/public`, без входа)

Самая чувствительная с точки зрения данных часть — единственное место,
которое реально общедоступно, поэтому нужен явный **список того, что можно
показывать** (белый список полей), а не «показать всё, кроме»:

**Можно показывать:**

- Список продуктов: `Product.name`, `Product.stage`, `Product.publicSummary`
  (или обрезанный `description`, если пусто), имя и роль `owner` (`Person`).
- `RoadmapItem`, только там где `visibility = PUBLIC` — заголовок, статус,
  квартал. **Без описания** по умолчанию (описание пункта роадмапа обычно
  содержит внутренние детали реализации, даже если сам факт «планируется X»
  безопасен публично) — если понадобится показывать описание, это
  отдельное решение по каждому пункту, не общий переключатель.

**Нельзя показывать ни при каких обстоятельствах** (явно фиксируется здесь,
чтобы не решать это ситуативно в коде каждой отдельной страницы):
`Research`/`Conversation` (транскрипты клиентов), `Insight`, `Hypothesis`,
`Competitor` (конкурентная разведка — данные о конкурентах не для внешнего
и внутрикорпоративного открытого распространения тоже), внутренние
`RoadmapItem` (`visibility = INTERNAL`), `ProcessStep`/`ProcessEdge`
(внутренняя кухня команды), `ProductOpportunity` (стратегические заметки
CPO).

**Технически:** отдельный публичный Server Component без проверки сессии
(её и так нет ни у кого сегодня, но публичный роут должен быть написан так,
будто она появится завтра — запросы явно `select`-ят только перечисленные
поля, а не берут модель целиком и не полагаются на то, что чувствительные
поля «просто не отрендерятся» в JSX). Это первое место в приложении, где
разница между «поле не показано в UI» и «поле не запрошено у БД» реально
имеет значение для безопасности, а не только для производительности.

## 5. CPO-представление (`/cpo`)

- **Все продукты, обзорная сетка** — переиспользует `ModuleRail`-подобный
  паттерн, но по продуктам, не по разделам одного продукта.
- **Экосистема / корреляции.** Сначала — вычисляемые (не хранимые)
  пересечения: продукты, у которых есть сегменты с одинаковым/похожим
  `Segment.name`, или JTBD с общей `category` — простой `groupBy`-запрос
  того же типа, что уже есть в `lib/dashboard-metrics.ts`, просто без
  фильтра по одному продукту. `ProductOpportunity` (см. §1) добавляется
  только если автоматических пересечений недостаточно, чтобы CPO мог
  зафиксировать словами возможность, которую формулы не увидят — это
  сознательно вынесено в отдельную, более позднюю фазу (§8, Фаза 5),
  чтобы не строить ручную аннотацию раньше, чем понятно, нужна ли она
  вообще поверх автоматики.
  - **Открытый вопрос:** «похожесть» сегментов/категорий по точному
    совпадению имени — грубый критерий (разные PM могут по-разному назвать
    один и тот же сегмент). Более умный вариант (общие теги, fuzzy-match)
    можно добавить позже — предложено начать с точного совпадения, потому
    что это ноль новой инфраструктуры и уже покажет, полезна ли сама идея.
- **Ключевые направления развития** — кросс-продуктовая версия уже
  существующего `getGapsCounts`/`getHypothesisStatusCounts` (`lib/
dashboard-metrics.ts`) — те же 4 «пробела» и воронка гипотез, но
  агрегированные по всем продуктам сразу с разбивкой по продукту, плюс
  разбивка `RoadmapItem` по статусу на продукт (сколько в работе, сколько
  запланировано, сколько зависло в `PLANNED` неоправданно долго — тот же
  принцип «зависшая гипотеза 14+ дней», перенесённый на роадмап).
- **Мультипродуктовый роадмап** — тот же `RoadmapItem`, но без фильтра по
  продукту, сгруппированный по кварталу с продуктом как второй осью —
  визуально таблица «квартал × продукт», не таймлайн одного продукта.

## 6. Маркетинг-инструменты (`/marketing-hub`, чтобы не путать с уже

существующим `/marketing` — CRUD-разделом RTB)

Наименее рискованная часть плана: почти вся нужная информация уже есть в
существующих моделях, это по сути новая read-only страница-компоновка
поверх уже собранных данных, без новой схемы (кроме использования
`RoadmapItem` для «что скоро выходит»):

- Фильтр по сегменту → показывает: какие `JTBD` этого сегмента подтверждены
  исследованием, какие `Feature` их закрывают, какие `RTB` опираются на эти
  фичи — то есть готовый «а что мы вообще можем сказать клиентам этого
  сегмента» без ручного пролистывания трёх разделов.
- Отдельная лента «скоро» — `RoadmapItem` со статусом `IN_PROGRESS`/
  `PLANNED`, чтобы маркетинг видел, что скоро можно будет анонсировать,
  до того как это стало `SHIPPED` фичей.

## 7. Продажи (`/sales-hub`)

Тоже почти без новой схемы — уже существующие `ProductResource` (sales-kit,
слайды) и `Feature` дают почти всё нужное; `RoadmapItem` закрывает
недостающий кусок «фичи ещё нет, но она запланирована»:

- Быстрый вход по продукту → сразу список `ProductResource` (kind =
  `SALES_KIT` в первую очередь, остальные ниже).
- Поиск «есть ли у нас фича X» — расширение уже существующего глобального
  `contains`-поиска (`app/search/page.tsx`) на `Feature` и `RoadmapItem`
  сразу, с явной пометкой в результате: «уже есть» (`Feature` существует)
  vs. «запланировано на 2026 Q3, статус: в работе» (`RoadmapItem`, если
  подходящей `Feature` ещё нет) vs. «не найдено» — три разных ответа на
  один и тот же вопрос продажника, каждый требует разного следующего шага
  с клиентом.
  - **Открытый вопрос дизайна:** показывать точную дату/квартал продажнику
    напрямую рискованно (квартал легко превращается в обещание клиенту) —
    альтернатива: показывать только качественный статус («в работе» /
    «запланировано» / «пока не в планах») без даты, а точный квартал —
    только во внутренних `/pm`/`/cpo` представлениях. Предложено как
    дефолт, требует подтверждения перед Фазой 7.

## 8. Порядок фаз

Зависимости определяют порядок сильнее желаемого приоритета — например,
командный дашборд PM без роадмапа показывать было бы почти нечего, поэтому
роадмап идёт раньше:

0. **Фундамент** ✅ сделано 8 августа 2026, ветка `2.0`. Модель `Person`
   (`/people` — список, карточка с инлайн-редактированием, CRUD-экшены
   `lib/actions/people.ts`; без `createPersonQuick` и без дублирования/CSV —
   осознанно урезано до того, что реально используется в Фазе 0, добавится
   когда появится реальный сценарий); поля `Product.ownerId` (select в форме
   продукта + инлайн-поле на детальной странице продукта, с обратной
   ссылкой — карточка `Person` показывает продукты, за которые он отвечает)
   и `Product.publicSummary`; каркас переключателя ролей в `SiteNav`
   («Представления» → `/pm`, `/cpo`, `/public`, `/marketing-hub`,
   `/sales-hub` — все пять пока `PersonaStub`-заглушки со списком того, что
   появится по фазам). «Люди» добавлены подпунктом меню «Продукты».
   Проверено: `npm run lint && npm run typecheck` чисто, 21 unit +
   110 integration + полный E2E зелёные, включая новый `e2e/people.spec.ts`
   (создание/инлайн-редактирование/удаление человека, назначение
   ответственного за продукт, навигация через переключатель ролей).
1. **Роадмап** ✅ сделано 8 августа 2026, ветка `2.0`. Модель `RoadmapItem`
   (статус PLANNED/IN_PROGRESS/SHIPPED/PAUSED — тот же 4-тоновый паттерн,
   что и у статусов гипотезы; видимость INTERNAL/PUBLIC; опциональные связи
   с `Person`/`Feature`/`JTBD`) + CRUD-экшены (`lib/actions/roadmap.ts`, без
   отдельной detail-страницы — тот же паттерн, что у `ProductResource`,
   создание/редактирование/удаление ведут обратно на `/pm`). `/pm` стал
   настоящей страницей: переключатель продукта (то же поле `localStorage`,
   что уже используется формами для «продукта по умолчанию» — тот же смысл,
   не отдельный ключ), список пунктов роадмапа выбранного продукта,
   сгруппированный по кварталу, с бейджем статуса и закреплением. Блоки
   «Команда» и «Процесс» на той же странице — пока честные заглушки
   «появится в Фазе 2/3», не пустые вкладки.
   Проверено: `npm run lint && npm run typecheck` чисто, интеграционные
   тесты на `lib/actions/roadmap.ts`, `e2e/pm-roadmap.spec.ts` (добавление
   пункта роадмапа, группировка по кварталу, удаление, память переключателя
   продукта), полный набор автотестов зелёный.
2. **Командный дашборд / матрица делегирования** ✅ сделано 8 августа 2026,
   ветка `2.0`. Реализован дефолт из открытого вопроса §3 без изменений:
   человек × текущая нагрузка. `lib/team-workload.ts`'s
   `getProductTeamWorkload(userId, productId)` — люди, у которых есть хотя
   бы один пункт роадмапа этого продукта (не весь справочник `Person`),
   с числом активных (PLANNED/IN_PROGRESS) и всего пунктов, отсортированные
   по активным по убыванию. Шаги процесса в нагрузку пока не входят — сам
   `ProcessStep` появляется только в Фазе 3; когда появится, добавится
   вторым слагаемым в ту же функцию. Секция «Команда» на `/pm` — реальная,
   не заглушка; пустое состояние — «пока никто не назначен».
   Проверено: интеграционные тесты на `getProductTeamWorkload` (сортировка,
   исключение пунктов без ответственного, исключение чужих продуктов),
   `e2e/pm-roadmap.spec.ts` (назначение ответственного → нагрузка видна),
   весь набор автотестов зелёный.
3. **Диаграмма процесса + экшн-планы** ✅ сделано 8 августа 2026, ветка
   `2.0`. Модели `ProcessStep`/`ProcessEdge` (без `userId` — тот же
   паттерн, что у `JtbdGraphLayout`/`JtbdSequenceEdge`, скоуп через цепочку
   `productId → Product → userId`) + новый канвас-компонент
   `components/process-graph/` на той же связке React Flow/`@xyflow/react`,
   что и JTBD-граф, но заметно проще: один канонический layout на продукт
   (позиция сохраняется прямо на `ProcessStep.x/y`), без реparent-по-drag,
   без auto-arrange (шагов обычно немного — вручную двигать достаточно).
   Добавление шага — инлайн-панель как у JTBD; редактирование названия и
   ответственного — панель-инспектор по клику на узел (детальной страницы
   у `ProcessStep`, как и договаривались, нет); связь между шагами — тянуть
   от края узла к другому, с необязательной подписью через `window.prompt`
   (тот же класс нативных диалогов, что уже использует `DeleteButton`) —
   отдельную UI для подписи связи не строили. `getProductTeamWorkload`
   (Фаза 2) расширена: назначенный шаг процесса считается активной
   нагрузкой наравне с незавершённым пунктом роадмапа, как и было
   запланировано в Фазе 2.

   Модель `ActionPlan` и её список на `/pm` (список — основной путь по
   плану §3, не зависит от канваса технически; секция «Экшн-планы»
   физически выше «Процесса» на странице по той же причине). Шаги плана —
   `Textarea` по одному на строку (`lib/validation.ts`'s `toLines`, новый
   помощник рядом с `toTagsArray` — перенос строки, а не запятая, потому
   что это осмысленная последовательность действий, а не набор тегов).

   **Найденный и исправленный баг:** `useNodesState`/`useEdgesState`
   инициализируют внутреннее состояние React Flow только один раз, при
   первом рендере — без `useEffect`, синхронизирующего его с `initial`
   при каждом изменении пропсов (как уже сделано в JTBD-графе), канвас
   продолжал показывать устаревшие узлы после `router.refresh()` за
   созданием/удалением шага. Поймано E2E-тестом, не при ручной проверке —
   без него баг ушёл бы в коммит.

   Проверено: `npm run lint && npm run typecheck` чисто, интеграционные
   тесты на `process.ts` (создание/обновление/удаление шага, каскад
   удаления рёбер, батч-сохранение позиций, создание/отклонение
   самопетли/дубликата связи) и `action-plans.ts` (создание с
   упорядоченными шагами и тегами, обновление, удаление, закрепление),
   `e2e/pm-process.spec.ts` (добавление шага → редактирование через
   инспектор → удаление; создание экшн-плана с шагами и тегами →
   удаление), весь набор автотестов зелёный.

4. **Открытый дашборд компании** ✅ сделано 8 августа 2026, ветка `2.0`.
   `app/public/page.tsx` заменил `PersonaStub` — единственная страница
   приложения, написанная по белому списку полей (§4), а не по
   чёрному: оба запроса Prisma используют явный `select` только на
   разрешённые поля, а не забирают модель целиком с расчётом, что лишнее
   просто не отрендерится в JSX — так страница остаётся безопасной, даже
   когда (не «если») у приложения наконец появятся настоящие сессии.
   Секция «Продукты» — карточка на продукт: имя, бейдж стадии,
   `publicSummary` (или обрезанное до 200 символов `description`, если
   `publicSummary` пусто), имя и роль `owner` (`Person`). Секция «Публичный
   роадмап» — только `RoadmapItem` с `visibility = PUBLIC`, сгруппированные
   по кварталу (тот же паттерн, что и группировка на `/pm`, изначально
   как локальная функция — вынесена в `lib/roadmap.ts` в Фазе 5, когда
   тот же код понадобился в третий раз на `/cpo`), с именем продукта на
   каждой строке (в отличие от `/pm`, страница не привязана к одному
   продукту), без описания пункта — только заголовок, статус, квартал,
   как и зафиксировано в §4. `Research`, `Conversation`, `Insight`,
   `Hypothesis`, `Competitor`, внутренние `RoadmapItem`, `ProcessStep`/
   `ProcessEdge` на странице не появляются и не запрашиваются вовсе.
   Проверено: `npm run lint && npm run typecheck` чисто,
   `e2e/public-dashboard.spec.ts` (продукт с публичным описанием и
   ответственным виден, публичный пункт роадмапа виден, внутренний —
   нет), весь набор автотестов зелёный: 25 unit + 135 integration + 33
   E2E.
5. **CPO-представление** ✅ сделано 8 августа 2026, ветка `2.0`.
   `app/cpo/page.tsx` заменил `PersonaStub`; вся логика — в новом
   `lib/cpo-metrics.ts`, отдельно от `lib/dashboard-metrics.ts`/
   `lib/team-workload.ts`, потому что ни одна функция там не скоуп
   по одному продукту — наоборот, каждая явно агрегирует по всем
   продуктам пользователя сразу, что было бы легко перепутать, окажись
   она в файле, где почти всё остальное принимает `productId?`.
   Четыре секции:
   - «Продукты» — обзорная сетка (карточка на продукт: имя, бейдж
     стадии, доля подтверждённых JTBD, число активных гипотез), ссылка
     на `/pm?productId=...`.
   - «Экосистема» — вычисляемые (не хранимые) пересечения: группы
     продуктов с сегментом одинакового имени и группы с общей
     категорией JTBD, точное совпадение строки — грубый критерий,
     осознанно выбранный дефолт (см. §5's открытый вопрос: разные PM
     могут по-разному назвать один и тот же сегмент, более умный
     fuzzy-match — возможное продолжение, не построено заранее).
     `ProductOpportunity` (ручная аннотация, см. §1) не построена —
     сначала нужно увидеть, достаточно ли автоматики, ручная разметка
     впрок не строится.
   - «Ключевые направления развития» — та же логика, что у
     `getGapsCounts`/`getHypothesisStatusCounts`
     (`lib/dashboard-metrics.ts`), но с разбивкой по продукту (эти
     функции уже не фильтруют по продукту сегодня — тут их результаты
     просто перегруппированы по продукту, без новых запросов), плюс
     разбивка `RoadmapItem` по статусу на продукт и «зависшие» пункты в
     `PLANNED` дольше 45 дней (тот же принцип, что у зависшей гипотезы
     14+ дней, но с более длинным окном — роадмап живёт кварталами, а
     не спринтами).
   - «Мультипродуктовый роадмап» — все `RoadmapItem` всех продуктов,
     сгруппированные по кварталу с именем продукта на каждой строке —
     тот же список-с-подписью-продукта, что и на `/public`, а не
     буквальная HTML-таблица «квартал × продукт»: тот же объём
     информации, меньше нового UI.
     `groupByQuarter` (была локальной в `/pm`, затем скопирована в
     `/public`) вынесена в `lib/roadmap.ts` — с третьим потребителем
     copy-paste перестал быть дешевле абстракции (rule of three);
     `/pm` и `/public` теперь импортируют её оттуда.
     Проверено: `npm run lint && npm run typecheck` чисто,
     `tests/integration/cpo-metrics.test.ts` (6 тестов — покрытие/гипотезы
     по продукту, пересечение сегментов/категорий через 2+ продукта,
     разбивка пробелов по продукту, зависшие PLANNED-пункты, группировка
     мультипродуктового роадмапа), `e2e/cpo.spec.ts` (сегмент и категория
     JTBD с одинаковым именем в двух продуктах видны как пересечение,
     пункт роадмапа виден в мультипродуктовом списке), весь набор
     автотестов зелёный: 25 unit + 141 integration + 34 E2E.
6. **Маркетинг-хаб** ✅ сделано 8 августа 2026, ветка `2.0`.
   `app/marketing-hub/page.tsx` заменил `PersonaStub` — как и обещано в
   §6, почти целиком композиция уже существующих данных: никакой новой
   Prisma-модели, никакого нового `lib/*-metrics.ts` — две прямые
   выборки прямо в странице (в отличие от `/cpo`, где агрегирующей
   логики достаточно, чтобы оправдать отдельный файл). Фильтр — по
   сегменту (не по продукту, как везде в приложении), новый
   `MarketingSegmentFilterForm`: имя продукта в каждой опции («Продукт —
   Сегмент»), потому что имя сегмента само по себе не уникально между
   продуктами — та же коллизия имён, которую `lib/cpo-metrics.ts`
   в Фазе 5 использует нарочно (совпадающие имена = вычисляемое
   пересечение), здесь просто мешает выбрать нужный сегмент. По
   умолчанию выбран первый сегмент (по имени продукта, потом по имени
   сегмента) — та же логика умолчания, что у `/reports/segments-jtbd`.
   Секция «Что можно сказать сегменту N» — JTBD этого сегмента (бейдж
   «Подтверждён», если подтверждён исследованием) → закрывающие их
   `Feature` → `RTB`, которые опираются на эти фичи — цепочка через уже
   существующие many-to-many связи, без единого нового запроса вне
   стандартного `include`. Секция «Скоро» — `RoadmapItem` продукта
   выбранного сегмента со статусом `PLANNED`/`IN_PROGRESS`.
   **Найденный и исправленный баг, не специфичный для этой фазы:**
   `components/ui/select.tsx`'s auto-submitting `onChange` (уже
   используемый `ReportsProductFilterForm`, теперь и здесь) отправлял
   форму со _старым_ значением, когда вариантов больше двух — Radix
   вызывает `onChange` синхронно, до того как React успевает
   перерендерить скрытый `<select>`-зеркало с новым `value`, так что
   `e.currentTarget.form?.requestSubmit()` читал устаревшее значение
   из DOM. Исправлено записью значения в скрытый `<select>`
   императивно, до вызова `onChange`, а не только через React-проп —
   гонка воспроизводилась детерминированно в отладочном скрипте и,
   вероятно, уже была скрытой проблемой `ReportsProductFilterForm`,
   просто не пойманной существующими тестами (там нет сценария с 3+
   вариантами и переключением между непервым и непервым). Проверено на
   всём наборе E2E, не только на `marketing-hub`, — регрессий нет.
   Проверено: `npm run lint && npm run typecheck` чисто,
   `e2e/marketing-hub.spec.ts` (JTBD → фича → RTB видны для сегмента,
   `RoadmapItem` в статусе PLANNED виден как «скоро»), весь набор
   автотестов зелёный: 25 unit + 141 integration + 35 E2E.
7. **Продажи-хаб** ✅ сделано 8 августа 2026, ветка `2.0`.
   `app/sales-hub/page.tsx` заменил `PersonaStub`. Переключатель продукта
   — `SalesProductSwitcher`, второй почти-дословный клон
   `PmProductSwitcher` (тот же паттерн "продукт по умолчанию" из
   `lib/client-storage.ts`, но указывает на `/sales-hub`) — второй
   потребитель этого кода, не третий, так что по той же логике, что уже
   применена к `groupByQuarter` (см. Фазу 5), пока не вынесен в общий
   компонент с `basePath`-пропом; будет обоснованно вынести, если
   появится третий.
   Секция «Материалы» — `ProductResource` выбранного продукта,
   sales-kit всегда первым (сортировка по тому же порядку, в котором
   `ProductResourceKind` объявлен в `schema.prisma`), а не по порядку
   создания записей.
   Секция «Есть ли у нас фича X?» — **не** буквальное расширение
   `/search` (план §7 предлагал это как первый вариант, но
   sales-специфичные правила — три разных ответа на один вопрос,
   обязательное скрытие даты — не вписываются в устройство общей
   `/search`, которая просто перечисляет совпадения по 10 моделям
   единообразно, без интерпретации: превращать её в место с особым
   поведением для одной аудитории значило бы усложнить код, которым
   пользуются все остальные разделы). Вместо этого — отдельная форма
   прямо на `/sales-hub`, скоуп на выбранный продукт, с тем же
   `contains`-поиском, что и у `/search`, но с явной трёхвариантной
   интерпретацией результата, а не сырым списком: **зелёная галка
   «уже есть»**, если находится `Feature`; иначе — **качественный
   статус без даты** («в работе» / «запланировано»), если находится
   `RoadmapItem` в `IN_PROGRESS`/`PLANNED` (сознательно без квартала —
   см. открытый вопрос §7, подтверждено продакт-менеджером явно перед
   стартом фазы: показывать только статус, точный квартал — только во
   внутренних `/pm`/`/cpo`); иначе — **«не найдено»**. `SHIPPED` и
   `PAUSED` не участвуют в поиске: выпущенное уже должно быть видно как
   `Feature`, а приостановленное не стоит анонсировать продажнику как
   «скоро».
   Проверено: `npm run lint && npm run typecheck` чисто,
   `e2e/sales-hub.spec.ts` (sales-kit сортируется первым независимо от
   порядка создания; поиск существующей фичи даёт «уже есть»; поиск
   пункта роадмапа даёт качественный статус без утечки квартала в
   разметку; поиск несуществующего — «не найдено»), весь набор
   автотестов зелёный: 25 unit + 141 integration + 36 E2E.

Все 8 фаз плана (0–7) сделаны на ветке `2.0` по состоянию на 8 августа
2026 — все пять представлений (`/pm`, `/cpo`, `/public`, `/marketing-hub`,
`/sales-hub`) теперь настоящие страницы, ни одна не заглушка.

## 9. Дополнение (после Фазы 7): Гант-диаграмма роадмапа

Прямой запрос продакт-менеджера после завершения всех 8 фаз: роадмап
на `/pm` должен уметь выглядеть как диаграмма Ганта — с параллельными
дорожками и отдельными метками конкретных версий. Не новая фаза плана
(не создаёт новое представление), а расширение уже существующей секции
«Роадмап» на `/pm` — сделано в тот же день, отдельным коммитом.

- **`RoadmapItem` — 5 новых полей**, все опциональные (существующие
  записи без них по-прежнему работают, просто не попадают на Гант):
  `trackGroup`/`track` — свободный текст, два уровня вложенности («Блок»
  → «Дорожка», например «Разработка» → «Фронт»/«Бэк»), та же философия,
  что у `quarter` — PM сам решает, что считать параллельным процессом,
  а не выбирает из фиксированного списка. `startDate`/`endDate` — даты
  полосы. `isMilestone` — если true, `startDate` становится датой
  вехи (например, релиза версии), рисуется не полосой, а сплошной
  вертикальной линией на всю высоту диаграммы с подписью сверху;
  `endDate`/`track`/`trackGroup` для вехи не используются. Продакт-
  менеджер явно выбрал чек-бокс на существующей модели вместо отдельной
  модели `Milestone` — веха семантически другая сущность, но недостаточно
  другая, чтобы окупить отдельную модель/CRUD/форму.
- **`lib/roadmap-gantt.ts`** — чистая функция `buildGanttLayout`,
  группирует полосы по `trackGroup` → `track` (с теми же
  «Без блока»/«Без трека» заглушками, что и `NO_QUARTER_LABEL` в
  `lib/roadmap.ts`), отделяет вехи, считает диапазон дат диаграммы с
  отступом по краям. 6 unit-тестов.
- **`components/roadmap-gantt/gantt-chart.tsx`** — серверный компонент
  (без клиентского состояния, единственная интерактивность — нативный
  `title`-тултип на полосе). Сделан по процедуре скилла dataviz:
  статусная палитра — те же `--signal-*` тона, что и везде в приложении;
  полоса — светлая заливка/тёмный текст, как у `Badge`
  (не сплошная заливка с белым текстом — специально проверено на
  контраст ещё на этапе первого скриншота); подпись внутри полосы —
  только если по эвристике на основе длины строки помещается
  (`labelFitsInBar`, отсутствие клиентского layout-прохода не позволяет
  измерить текст по-настоящему), иначе полоса без подписи, только
  тултип — **найдено и исправлено во время ручной проверки**: первая
  версия рисовала подпись сбоку от полосы, и на плотной дорожке с двумя
  соседними пунктами подпись первого наезжала на полосу второго;
  вариант с обрезкой текста внутри узкой полосы (`Уча...`) тоже был
  отклонён по той же причине — оба нарушают явное правило скилла
  «label that won't fit doesn't get clipped». Легенда статусов, линия
  «Сегодня», подписанные метки версий на всю высоту.
  Скролл по горизонтали при узком экране (`min-w-[900px]`).
- **`/pm`** — новый переключатель «Список»/«Гант» (`RoadmapViewTabs`,
  тот же визуальный паттерн, что у `JtbdViewTabs`, но переключает query-
  параметр `view` на том же роуте, а не отдельный маршрут, потому что
  `/pm` уже держит состояние в `productId`).
  Проверено: `npm run lint && npm run typecheck` чисто,
  `tests/unit/roadmap-gantt.test.ts` (6 тестов), интеграционные тесты
  на новые поля `lib/actions/roadmap.ts` (создание полосы с
  треком/датами, создание вехи, отклонение `endDate < startDate`),
  `e2e/pm-roadmap-gantt.spec.ts` (переключение на Гант показывает блок,
  дорожку, полосу и веху), весь набор автотестов зелёный: 31 unit + 144
  integration + 37 E2E.

Каждая фаза — самостоятельно проверяемая и выкатываемая единица (по
образцу уже отработанного в `dashboard-redesign-plan.md`/
`visual-redesign-plan.md` ритма): `npm run lint && npm run typecheck`,
интеграционные тесты на новые Server Actions, E2E на новый флоу,
скриншоты, обновление `PROJECT_STATE.md`/`docs/user-guide/` там, где фаза
меняет то, что видит пользователь.

## 9. Явно вне рамок этого плана

- **Настоящая многопользовательская авторизация с разграничением доступа
  по ролям** (PM физически не видит чужие продукты, маркетинг не может
  редактировать роадмап и т.д.) — переключатель ролей в этой фазе не
  является границей безопасности, см. §2. Это следующий, отдельный и
  крупный план, если/когда концепция 5 представлений подтвердится
  использованием.
- **`Person` как логин-аккаунт** — не входит в рамки; если позже
  понадобится, чтобы человек из матрицы делегирования сам логинился и
  видел «свои» назначенные пункты роадмапа, это отдельное решение,
  завязанное на предыдущий пункт.
- **Полноценный таск-трекер** — `RoadmapItem`/`ProcessStep` с назначенным
  `Person` дают лёгкую видимость «кто чем занят», не замену Jira/Linear
  (нет подзадач, нет трудозатрат, нет спринтов).
- **Полноценная система инцидент-менеджмента** (эскалации, SLA, статус
  «сейчас идёт реагирование», уведомления) — `ActionPlan` (§3) сознательно
  остаётся статичным текстом-инструкцией, написанным заранее и спокойно, а
  не live-инструментом отслеживания текущего инцидента.
- **ML/кластеризация для поиска «возможностей интеграции»** между
  продуктами (§5) — только детерминированные правила (общий сегмент/
  категория JTBD) в первой итерации.
- **Мультитенантность/workspace-модель на компанию** — по-прежнему
  однопользовательская MVP-модель под капотом (`plans/backlog.md`); пять
  представлений в этом плане — разные _линзы_ на одни и те же данные, не
  разные организации.

## 10. Открытые вопросы, требующие подтверждения перед соответствующей фазой

Сведены здесь для удобства (каждый уже отмечен в контексте своего раздела
выше):

- §3 — точные оси «стратегической матрицы» команды (по умолчанию: человек ×
  текущая нагрузка).
- §5 — критерий «похожести» сегментов/категорий для авто-корреляций
  (по умолчанию: точное совпадение имени/категории).
- §7 — показывать ли точный квартал продажникам напрямую или только
  качественный статус (по умолчанию: только качественный статус, точная
  дата — во внутренних представлениях).

## 11. Дополнение (после Гант-диаграммы §9): департаменты, кросс-продуктовый Гант, множественные процессы

Прямой запрос продакт-менеджера, три связанных изменения:

**Департаменты.** `/cpo` — единственное место, где продукты рассматриваются
как экосистема, но у CPO не было способа группировать их по стратегическим
направлениям («MFA-продукты», «Электронная подпись», «IoT-безопасность»).
Решение — не свободный текст (как `RoadmapItem.trackGroup`/`quarter`), а
отдельная модель `Department` (`id`, `name`, `color`, `description`,
`userId` — тот же паттерн, что `Person`: сущность актуальна сразу для
нескольких продуктов, поэтому не привязана к одному `productId`).
`Product.departmentId` — опциональная связь, отдельная ось от `userId`/
`ownerId`, `onDelete: SetNull` (удаление департамента не удаляет продукты).
Полноценный CRUD: `/departments` (список — цветная точка + название +
число продуктов), `/departments/new`, `/departments/[id]` (детальная
страница с инлайн-редактированием имени/описания и списком продуктов
департамента), `/departments/[id]/edit`. Ссылка «Департаменты» — новый
подпункт в подменю «Продукты» рядом с Фичи/Конкуренты/Люди. `ProductForm`
получил select-поле «Департамент» (`Не указан` по умолчанию, тот же паттерн
optional-relation, что и `ownerId`), детальная страница продукта — такое же
инлайн-редактируемое поле рядом с «Ответственный».

**Кросс-продуктовый Гант на `/cpo`.** Секция «Мультипродуктовый роадмап»
получила переключатель Список/Гант (`MultiRoadmapViewTabs` —
намеренный близкий дубликат `RoadmapViewTabs` с `/pm`, 2-й потребитель того
же паттерна, не обобщён в общий компонент). Ключевое архитектурное
решение: `getMultiProductGanttLayout()` (`lib/cpo-metrics.ts`) **не
дублирует** логику раскладки Ганта — она мапит каждый `RoadmapItem` в уже
существующий `GanttSourceItem` (`lib/roadmap-gantt.ts`), подставляя вместо
собственных `trackGroup`/`track` пункта его департамент/продукт, и передаёт
результат в уже существующий `buildGanttLayout()`. Функция группировки не
знает и не обязана знать, что теперь «трек» — это целый продукт, а не
дорожка внутри одного продукта; это ровно то же дерево
блок→дорожка→бары/вехи. Единственная содержательная доработка — вехи
получают суффикс с именем продукта (`v2.0 (Продукт Б)`), поскольку
вертикальная линия вехи проходит через все дорожки сразу и, в отличие от
бара, не привязана визуально к своему треку. `getProductsOverview()`
дополнена `department`, `groupByDepartment()` группирует и сортирует
результат (по алфавиту, без департамента — всегда последними,
`NO_DEPARTMENT_LABEL`), секция «Продукты» теперь рендерит сетку продуктов
внутри подзаголовков-департаментов, а не одной плоской сеткой.

**Множественные процессы на продукт.** Раньше `ProcessStep`/`ProcessEdge`
были привязаны напрямую к `productId` — один канонический процесс на
продукт. Запрос: «сначала список всех описанных процессов, и только потом
разные графы — для разных процессов» (например, «Запуск маркетинговой
кампании» и «Обработка инцидента безопасности» — два независимых потока).
Решение — новая модель `Process` (`id`, `title`, `productId`) между
`Product` и `ProcessStep`: `ProcessStep.processId` заменил
`ProcessStep.productId`, каждый шаг принадлежит ровно одному процессу (не
множеству — открытый вопрос плана закрыт в пользу более простого варианта
по прямому ответу продакт-менеджера). Секция «Процесс» на `/pm` теперь
двухуровневая через query-параметр `processId`, тот же паттерн, что
`view=list|gantt` у роадмапа: без `processId` — hairline-список процессов
продукта (название + число шагов, «Добавить процесс»), с валидным
`processId` — canvas этого процесса (`ProcessGraph`, теперь принимает
`processId` вместо `productId`) плюс «← Все процессы»/«Переименовать»/
«Удалить» в шапке секции. CRUD самого `Process` — `lib/actions/processes.ts`
(отдельно от `lib/actions/process.ts`, который управляет шагами/связями
внутри уже выбранного процесса), `/pm/processes/new`,
`/pm/processes/[id]/edit` — тот же «без отдельной detail-страницы» паттерн,
что `RoadmapItem`/`ActionPlan`: редирект обратно на `/pm?productId=X` (после
удаления) или `/pm?productId=X&processId=Y` (после создания/переименования
— сразу в canvas). `lib/team-workload.ts` и оба места, читающие шаги
процесса для пикера в форме экшн-плана, обновлены на вложенный фильтр
`where: { process: { productId } }` вместо прямого `productId` на
`ProcessStep`.

Найден и исправлен попутный баг: `pluralizeProducts()` (родился в Фазе 5
как локальная функция `/cpo`) правильно склонял «продукт»/«продукта», но
на «Департаментах» и в счётчике шагов на `/pm` то же самое было заново
написано вручную и неправильно (просто «1 vs остальное», без формы для
2–4). Вынесено в общий `pluralizeRu(count, [singular, few, many])`
(`lib/utils.ts`), переиспользуется на `/cpo`, `/departments`, `/pm`.

Тесты: `lib/actions/departments.ts`, `lib/actions/processes.ts`,
`lib/cpo-metrics.ts` (`getMultiProductGanttLayout`, `groupByDepartment`),
`pluralizeRu` — все интеграционным+unit-слоем; E2E —
`e2e/departments.spec.ts` (создание департамента → привязка продукта →
проверка на `/cpo` списком и Гантом → переименование → удаление) и
переработанный `e2e/pm-process.spec.ts` (создание процесса → шаг →
переименование/удаление шага → переименование/удаление процесса).
